function generate298Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.375 * width, y: y + 0.481 * height, angle: 4.6 }, // Start/Finish
        { x: x + 0.293 * width, y: y + 0.242 * height }, // Checkpoint 2
        { x: x + 0.316 * width, y: y + 0.086 * height }, // Checkpoint 3
        { x: x + 0.363 * width, y: y + -0.059 * height }, // Checkpoint 4
        { x: x + 0.444 * width, y: y + -0.175 * height }, // Checkpoint 5
        { x: x + 0.557 * width, y: y + -0.208 * height }, // Checkpoint 6
        { x: x + 0.665 * width, y: y + -0.150 * height }, // Checkpoint 7
        { x: x + 0.749 * width, y: y + -0.009 * height }, // Checkpoint 8
        { x: x + 0.720 * width, y: y + 0.392 * height }, // Checkpoint 9
        { x: x + 0.918 * width, y: y + 0.374 * height }, // Checkpoint 10
        { x: x + 1.016 * width, y: y + 0.485 * height }, // Checkpoint 11
        { x: x + 1.039 * width, y: y + 0.623 * height }, // Checkpoint 12
        { x: x + 0.987 * width, y: y + 0.795 * height }, // Checkpoint 13
        { x: x + 0.894 * width, y: y + 0.888 * height }, // Checkpoint 14
        { x: x + 0.682 * width, y: y + 0.951 * height }, // Checkpoint 15
        { x: x + 0.593 * width, y: y + 0.943 * height }, // Checkpoint 16
        { x: x + 0.533 * width, y: y + 0.890 * height }, // Checkpoint 17
        { x: x + 0.326 * width, y: y + 0.949 * height }, // Checkpoint 18
        { x: x + 0.206 * width, y: y + 0.924 * height }, // Checkpoint 19
        { x: x + 0.083 * width, y: y + 0.674 * height }, // Checkpoint 20
        { x: x + 0.086 * width, y: y + 0.581 * height }, // Checkpoint 21
        { x: x + 0.119 * width, y: y + 0.534 * height }, // Checkpoint 22
        { x: x + 0.243 * width, y: y + 0.485 * height }, // Checkpoint 23
        { x: x + 0.423 * width, y: y + 0.469 * height }, // Back to Start
    ];
}

function generate299Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.151 * width, y: y + 0.995 * height, angle: 3.3 }, // Start/Finish
        { x: x + 0.003 * width, y: y + 0.951 * height }, // Checkpoint 2
        { x: x + -0.067 * width, y: y + 0.793 * height }, // Checkpoint 3
        { x: x + -0.057 * width, y: y + 0.606 * height }, // Checkpoint 4
        { x: x + 0.037 * width, y: y + 0.433 * height }, // Checkpoint 5
        { x: x + 0.274 * width, y: y + 0.142 * height }, // Checkpoint 6
        { x: x + 0.522 * width, y: y + -0.043 * height }, // Checkpoint 7
        { x: x + 0.750 * width, y: y + -0.146 * height }, // Checkpoint 8
        { x: x + 0.901 * width, y: y + -0.130 * height }, // Checkpoint 9
        { x: x + 0.956 * width, y: y + -0.024 * height }, // Checkpoint 10
        { x: x + 0.941 * width, y: y + 0.317 * height }, // Checkpoint 11
        { x: x + 1.071 * width, y: y + 0.568 * height }, // Checkpoint 12
        { x: x + 1.056 * width, y: y + 0.688 * height }, // Checkpoint 13
        { x: x + 0.970 * width, y: y + 0.771 * height }, // Checkpoint 14
        { x: x + 0.899 * width, y: y + 0.766 * height }, // Checkpoint 15
        { x: x + 0.820 * width, y: y + 0.675 * height }, // Checkpoint 16
        { x: x + 0.729 * width, y: y + 0.486 * height }, // Checkpoint 17
        { x: x + 0.631 * width, y: y + 0.433 * height }, // Checkpoint 18
        { x: x + 0.559 * width, y: y + 0.441 * height }, // Checkpoint 19
        { x: x + 0.404 * width, y: y + 0.552 * height }, // Checkpoint 20
        { x: x + 0.318 * width, y: y + 0.788 * height }, // Checkpoint 21
        { x: x + 0.208 * width, y: y + 0.957 * height }, // Back to Start
    ];
}

function generate300Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.472 * width, y: y + 0.778 * height, angle: 3.8 }, // Start/Finish
        { x: x + 0.184 * width, y: y + 0.637 * height }, // Checkpoint 2
        { x: x + 0.086 * width, y: y + 0.506 * height }, // Checkpoint 3
        { x: x + 0.099 * width, y: y + 0.418 * height }, // Checkpoint 4
        { x: x + 0.475 * width, y: y + 0.337 * height }, // Checkpoint 5
        { x: x + 0.538 * width, y: y + 0.127 * height }, // Checkpoint 6
        { x: x + 0.631 * width, y: y + -0.017 * height }, // Checkpoint 7
        { x: x + 0.695 * width, y: y + -0.063 * height }, // Checkpoint 8
        { x: x + 1.000 * width, y: y + -0.052 * height }, // Checkpoint 9
        { x: x + 1.062 * width, y: y + 0.011 * height }, // Checkpoint 10
        { x: x + 1.084 * width, y: y + 0.145 * height }, // Checkpoint 11
        { x: x + 1.050 * width, y: y + 0.323 * height }, // Checkpoint 12
        { x: x + 0.966 * width, y: y + 0.464 * height }, // Checkpoint 13
        { x: x + 1.089 * width, y: y + 0.631 * height }, // Checkpoint 14
        { x: x + 1.112 * width, y: y + 0.794 * height }, // Checkpoint 15
        { x: x + 1.086 * width, y: y + 0.874 * height }, // Checkpoint 16
        { x: x + 0.894 * width, y: y + 1.030 * height }, // Checkpoint 17
        { x: x + 0.703 * width, y: y + 1.026 * height }, // Checkpoint 18
        { x: x + 0.566 * width, y: y + 0.902 * height }, // Checkpoint 19
        { x: x + 0.496 * width, y: y + 0.848 * height }, // Back to Start
    ];
}

function generate301Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.186 * width, y: y + 0.353 * height, angle: 5.5 }, // Start/Finish
        { x: x + -0.169 * width, y: y + 0.266 * height }, // Checkpoint 2
        { x: x + -0.060 * width, y: y + 0.132 * height }, // Checkpoint 3
        { x: x + 0.135 * width, y: y + -0.072 * height }, // Checkpoint 4
        { x: x + 0.269 * width, y: y + -0.159 * height }, // Checkpoint 5
        { x: x + 1.043 * width, y: y + 0.407 * height }, // Checkpoint 6
        { x: x + 1.131 * width, y: y + 0.489 * height }, // Checkpoint 7
        { x: x + 1.152 * width, y: y + 0.581 * height }, // Checkpoint 8
        { x: x + 1.116 * width, y: y + 0.655 * height }, // Checkpoint 9
        { x: x + 0.933 * width, y: y + 0.715 * height }, // Checkpoint 10
        { x: x + 0.500 * width, y: y + 0.434 * height }, // Checkpoint 11
        { x: x + 0.421 * width, y: y + 0.396 * height }, // Checkpoint 12
        { x: x + 0.307 * width, y: y + 0.394 * height }, // Checkpoint 13
        { x: x + 0.262 * width, y: y + 0.456 * height }, // Checkpoint 14
        { x: x + 0.193 * width, y: y + 0.715 * height }, // Checkpoint 15
        { x: x + -0.007 * width, y: y + 0.878 * height }, // Checkpoint 16
        { x: x + -0.053 * width, y: y + 0.878 * height }, // Checkpoint 17
        { x: x + -0.115 * width, y: y + 0.821 * height }, // Checkpoint 18
        { x: x + -0.148 * width, y: y + 0.731 * height }, // Checkpoint 19
        { x: x + -0.174 * width, y: y + 0.375 * height }, // Back to Start
    ];
}

function generate302Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.510 * width, y: y + 0.516 * height, angle: 296 }, // Start/Finish
        { x: x + 0.603 * width, y: y + 0.214 * height }, // Checkpoint 2
        { x: x + 0.688 * width, y: y + 0.117 * height }, // Checkpoint 3
        { x: x + 0.811 * width, y: y + 0.101 * height }, // Checkpoint 4
        { x: x + 0.943 * width, y: y + 0.224 * height }, // Checkpoint 5
        { x: x + 1.019 * width, y: y + 0.212 * height }, // Checkpoint 6
        { x: x + 1.060 * width, y: y + 0.094 * height }, // Checkpoint 7
        { x: x + 0.988 * width, y: y + -0.080 * height }, // Checkpoint 8
        { x: x + 0.888 * width, y: y + -0.157 * height }, // Checkpoint 9
        { x: x + 0.772 * width, y: y + -0.181 * height }, // Checkpoint 10
        { x: x + 0.655 * width, y: y + -0.122 * height }, // Checkpoint 11
        { x: x + 0.459 * width, y: y + 0.067 * height }, // Checkpoint 12
        { x: x + 0.373 * width, y: y + 0.094 * height }, // Checkpoint 13
        { x: x + 0.264 * width, y: y + 0.031 * height }, // Checkpoint 14
        { x: x + 0.159 * width, y: y + -0.115 * height }, // Checkpoint 15
        { x: x + 0.037 * width, y: y + -0.107 * height }, // Checkpoint 16
        { x: x + -0.019 * width, y: y + 0.013 * height }, // Checkpoint 17
        { x: x + 0.053 * width, y: y + 0.270 * height }, // Checkpoint 18
        { x: x + -0.021 * width, y: y + 0.725 * height }, // Checkpoint 19
        { x: x + -0.004 * width, y: y + 0.816 * height }, // Checkpoint 20
        { x: x + 0.055 * width, y: y + 0.882 * height }, // Checkpoint 21
        { x: x + 0.171 * width, y: y + 0.916 * height }, // Checkpoint 22
        { x: x + 0.348 * width, y: y + 0.791 * height }, // Checkpoint 23
        { x: x + 0.510 * width, y: y + 0.830 * height }, // Checkpoint 24
        { x: x + 0.700 * width, y: y + 0.935 * height }, // Checkpoint 25
        { x: x + 0.951 * width, y: y + 0.904 * height }, // Checkpoint 26
        { x: x + 1.025 * width, y: y + 0.860 * height }, // Checkpoint 27
        { x: x + 1.087 * width, y: y + 0.701 * height }, // Checkpoint 28
        { x: x + 1.095 * width, y: y + 0.482 * height }, // Checkpoint 29
        { x: x + 0.975 * width, y: y + 0.454 * height }, // Checkpoint 30
        { x: x + 0.757 * width, y: y + 0.597 * height }, // Checkpoint 31
        { x: x + 0.628 * width, y: y + 0.584 * height }, // Checkpoint 32
        { x: x + 0.521 * width, y: y + 0.512 * height }, // Back to Start
    ];
}

function generate303Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.541 * width, y: y + 0.946 * height, angle: 0 }, // Start/Finish
        { x: x + 0.443 * width, y: y + 0.956 * height }, // Checkpoint 2
        { x: x + 0.227 * width, y: y + 0.895 * height }, // Checkpoint 3
        { x: x + 0.023 * width, y: y + 0.759 * height }, // Checkpoint 4
        { x: x + -0.088 * width, y: y + 0.515 * height }, // Checkpoint 5
        { x: x + -0.071 * width, y: y + 0.302 * height }, // Checkpoint 6
        { x: x + 0.003 * width, y: y + 0.217 * height }, // Checkpoint 7
        { x: x + 0.133 * width, y: y + 0.206 * height }, // Checkpoint 8
        { x: x + 0.331 * width, y: y + 0.395 * height }, // Checkpoint 9
        { x: x + 0.434 * width, y: y + 0.406 * height }, // Checkpoint 10
        { x: x + 0.855 * width, y: y + 0.046 * height }, // Checkpoint 11
        { x: x + 0.982 * width, y: y + 0.004 * height }, // Checkpoint 12
        { x: x + 1.071 * width, y: y + 0.103 * height }, // Checkpoint 13
        { x: x + 1.101 * width, y: y + 0.232 * height }, // Checkpoint 14
        { x: x + 1.044 * width, y: y + 0.605 * height }, // Checkpoint 15
        { x: x + 1.108 * width, y: y + 0.880 * height }, // Checkpoint 16
        { x: x + 1.059 * width, y: y + 0.954 * height }, // Checkpoint 17
        { x: x + 0.960 * width, y: y + 0.987 * height }, // Checkpoint 18
        { x: x + 0.675 * width, y: y + 0.946 * height }, // Checkpoint 19
        { x: x + 0.527 * width, y: y + 0.979 * height }, // Back to Start
    ];
}

function generate304Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.052 * width, y: y + 0.016 * height, angle: 89.5 }, // Start/Finish
        { x: x + 0.050 * width, y: y + 0.457 * height }, // Checkpoint 2
        { x: x + 0.045 * width, y: y + 0.821 * height }, // Checkpoint 3
        { x: x + 0.157 * width, y: y + 0.862 * height }, // Checkpoint 4
        { x: x + 0.281 * width, y: y + 0.811 * height }, // Checkpoint 5
        { x: x + 0.460 * width, y: y + 0.745 * height }, // Checkpoint 6
        { x: x + 0.760 * width, y: y + 0.756 * height }, // Checkpoint 7
        { x: x + 0.871 * width, y: y + 0.753 * height }, // Checkpoint 8
        { x: x + 0.919 * width, y: y + 0.691 * height }, // Checkpoint 9
        { x: x + 0.971 * width, y: y + 0.509 * height }, // Checkpoint 10
        { x: x + 0.967 * width, y: y + 0.332 * height }, // Checkpoint 11
        { x: x + 0.931 * width, y: y + 0.253 * height }, // Checkpoint 12
        { x: x + 0.843 * width, y: y + 0.182 * height }, // Checkpoint 13
        { x: x + 0.714 * width, y: y + 0.147 * height }, // Checkpoint 14
        { x: x + 0.614 * width, y: y + 0.215 * height }, // Checkpoint 15
        { x: x + 0.545 * width, y: y + 0.326 * height }, // Checkpoint 16
        { x: x + 0.317 * width, y: y + 0.345 * height }, // Checkpoint 17
        { x: x + 0.352 * width, y: y + -0.009 * height }, // Checkpoint 18
        { x: x + 0.202 * width, y: y + -0.074 * height }, // Checkpoint 19
        { x: x + 0.069 * width, y: y + -0.019 * height }, // Back to Start
    ];
}

function generate305Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.968 * width, y: y + 0.842 * height, angle: 179 }, // Start/Finish
        { x: x + 0.320 * width, y: y + 0.851 * height }, // Checkpoint 2
        { x: x + 0.249 * width, y: y + 0.987 * height }, // Checkpoint 3
        { x: x + 0.056 * width, y: y + 0.970 * height }, // Checkpoint 4
        { x: x + 0.037 * width, y: y + 0.358 * height }, // Checkpoint 5
        { x: x + 0.082 * width, y: y + 0.083 * height }, // Checkpoint 6
        { x: x + 0.130 * width, y: y + 0.053 * height }, // Checkpoint 7
        { x: x + 0.420 * width, y: y + 0.051 * height }, // Checkpoint 8
        { x: x + 1.035 * width, y: y + 0.170 * height }, // Checkpoint 9
        { x: x + 1.068 * width, y: y + 0.687 * height }, // Checkpoint 10
        { x: x + 0.961 * width, y: y + 0.832 * height }, // Back to Start
    ];
}
function generate307Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.071 * width, y: y + 0.887 * height, angle: 4 }, // Start/Finish
        { x: x + 0.030 * width, y: y + 0.640 * height }, // Checkpoint 2
        { x: x + 0.084 * width, y: y + 0.578 * height }, // Checkpoint 3
        { x: x + 0.164 * width, y: y + 0.562 * height }, // Checkpoint 4
        { x: x + 0.282 * width, y: y + 0.599 * height }, // Checkpoint 5
        { x: x + 0.448 * width, y: y + 0.777 * height }, // Checkpoint 6
        { x: x + 0.559 * width, y: y + 0.838 * height }, // Checkpoint 7
        { x: x + 0.720 * width, y: y + 0.866 * height }, // Checkpoint 8
        { x: x + 0.830 * width, y: y + 0.838 * height }, // Checkpoint 9
        { x: x + 0.903 * width, y: y + 0.728 * height }, // Checkpoint 10
        { x: x + 0.943 * width, y: y + 0.560 * height }, // Checkpoint 11
        { x: x + 0.965 * width, y: y + 0.257 * height }, // Checkpoint 12
        { x: x + 0.916 * width, y: y + 0.141 * height }, // Checkpoint 13
        { x: x + 0.851 * width, y: y + 0.100 * height }, // Checkpoint 14
        { x: x + 0.772 * width, y: y + 0.103 * height }, // Checkpoint 15
        { x: x + 0.441 * width, y: y + 0.149 * height }, // Checkpoint 16
        { x: x + 0.037 * width, y: y + 0.258 * height }, // Checkpoint 17
        { x: x + -0.118 * width, y: y + 0.433 * height }, // Checkpoint 18
        { x: x + -0.191 * width, y: y + 0.704 * height }, // Checkpoint 19
        { x: x + -0.191 * width, y: y + 0.862 * height }, // Checkpoint 20
        { x: x + -0.130 * width, y: y + 0.918 * height }, // Checkpoint 21
        { x: x + -0.075 * width, y: y + 0.890 * height }, // Back to Start
    ];
}

function generate306Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.013 * width, y: y + -0.033 * height, angle: 352 }, // Start/Finish
        { x: x + 0.346 * width, y: y + -0.082 * height }, // Checkpoint 2
        { x: x + 0.446 * width, y: y + 0.199 * height }, // Checkpoint 3
        { x: x + 0.791 * width, y: y + 0.261 * height }, // Checkpoint 4
        { x: x + 0.831 * width, y: y + 0.547 * height }, // Checkpoint 5
        { x: x + 0.885 * width, y: y + 0.623 * height }, // Checkpoint 6
        { x: x + 1.057 * width, y: y + 0.716 * height }, // Checkpoint 7
        { x: x + 1.072 * width, y: y + 0.754 * height }, // Checkpoint 8
        { x: x + 1.067 * width, y: y + 1.015 * height }, // Checkpoint 9
        { x: x + 0.975 * width, y: y + 1.033 * height }, // Checkpoint 10
        { x: x + 0.856 * width, y: y + 0.966 * height }, // Checkpoint 11
        { x: x + 0.667 * width, y: y + 0.928 * height }, // Checkpoint 12
        { x: x + 0.268 * width, y: y + 1.004 * height }, // Checkpoint 13
        { x: x + 0.141 * width, y: y + 0.501 * height }, // Checkpoint 14
        { x: x + -0.016 * width, y: y + 0.276 * height }, // Checkpoint 15
        { x: x + -0.094 * width, y: y + -0.012 * height }, // Checkpoint 16
        { x: x + -0.060 * width, y: y + -0.033 * height }, // Back to Start
    ];
}

function generate308Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.008 * width, y: y + 0.167 * height, angle: 6 }, // Start/Finish
        { x: x + -0.042 * width, y: y + 0.496 * height }, // Checkpoint 2
        { x: x + -0.049 * width, y: y + 0.885 * height }, // Checkpoint 3
        { x: x + 0.073 * width, y: y + 0.771 * height }, // Checkpoint 4
        { x: x + 0.451 * width, y: y + 0.227 * height }, // Checkpoint 5
        { x: x + 0.539 * width, y: y + 0.592 * height }, // Checkpoint 6
        { x: x + 0.523 * width, y: y + 0.888 * height }, // Checkpoint 7
        { x: x + 0.651 * width, y: y + 0.880 * height }, // Checkpoint 8
        { x: x + 0.785 * width, y: y + 0.757 * height }, // Checkpoint 9
        { x: x + 0.804 * width, y: y + 0.444 * height }, // Checkpoint 10
        { x: x + 0.794 * width, y: y + 0.349 * height }, // Checkpoint 11
        { x: x + 0.746 * width, y: y + 0.281 * height }, // Checkpoint 12
        { x: x + 0.601 * width, y: y + 0.074 * height }, // Checkpoint 13
        { x: x + 0.470 * width, y: y + -0.010 * height }, // Checkpoint 14
        { x: x + 0.277 * width, y: y + 0.044 * height }, // Checkpoint 15
        { x: x + 0.101 * width, y: y + 0.145 * height }, // Checkpoint 16
        { x: x + 0.027 * width, y: y + 0.150 * height }, // Back to Start
    ];
}

function generate309Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.108 * width, y: y + 0.007 * height, angle: 0 }, // Start/Finish
        { x: x + 0.080 * width, y: y + 0.037 * height }, // Checkpoint 2
        { x: x + 0.306 * width, y: y + 0.078 * height }, // Checkpoint 3
        { x: x + 0.546 * width, y: y + 0.059 * height }, // Checkpoint 4
        { x: x + 0.651 * width, y: y + 0.119 * height }, // Checkpoint 5
        { x: x + 0.849 * width, y: y + 0.121 * height }, // Checkpoint 6
        { x: x + 0.939 * width, y: y + 0.165 * height }, // Checkpoint 7
        { x: x + 1.037 * width, y: y + 0.317 * height }, // Checkpoint 8
        { x: x + 1.042 * width, y: y + 0.529 * height }, // Checkpoint 9
        { x: x + 0.961 * width, y: y + 0.777 * height }, // Checkpoint 10
        { x: x + 0.901 * width, y: y + 0.845 * height }, // Checkpoint 11
        { x: x + 0.811 * width, y: y + 0.870 * height }, // Checkpoint 12
        { x: x + 0.727 * width, y: y + 0.840 * height }, // Checkpoint 13
        { x: x + 0.677 * width, y: y + 0.706 * height }, // Checkpoint 14
        { x: x + 0.727 * width, y: y + 0.655 * height }, // Checkpoint 15
        { x: x + 0.799 * width, y: y + 0.603 * height }, // Checkpoint 16
        { x: x + 0.823 * width, y: y + 0.480 * height }, // Checkpoint 17
        { x: x + 0.689 * width, y: y + 0.396 * height }, // Checkpoint 18
        { x: x + 0.489 * width, y: y + 0.372 * height }, // Checkpoint 19
        { x: x + 0.423 * width, y: y + 0.524 * height }, // Checkpoint 20
        { x: x + 0.368 * width, y: y + 0.818 * height }, // Checkpoint 21
        { x: x + 0.249 * width, y: y + 0.905 * height }, // Checkpoint 22
        { x: x + 0.123 * width, y: y + 0.900 * height }, // Checkpoint 23
        { x: x + 0.049 * width, y: y + 0.791 * height }, // Checkpoint 24
        { x: x + 0.037 * width, y: y + 0.600 * height }, // Checkpoint 25
        { x: x + 0.215 * width, y: y + 0.546 * height }, // Checkpoint 26
        { x: x + 0.244 * width, y: y + 0.429 * height }, // Checkpoint 27
        { x: x + 0.139 * width, y: y + 0.353 * height }, // Checkpoint 28
        { x: x + -0.061 * width, y: y + 0.380 * height }, // Checkpoint 29
        { x: x + -0.135 * width, y: y + 0.301 * height }, // Checkpoint 30
        { x: x + -0.168 * width, y: y + 0.211 * height }, // Checkpoint 31
        { x: x + -0.104 * width, y: y + -0.017 * height }, // Back to Start
    ];
}

function generate310Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.016 * width, y: y + 0.017 * height, angle: 83 }, // Start/Finish
        { x: x + 0.118 * width, y: y + 0.860 * height }, // Checkpoint 2
        { x: x + 0.262 * width, y: y + 0.455 * height }, // Checkpoint 3
        { x: x + 0.424 * width, y: y + 0.835 * height }, // Checkpoint 4
        { x: x + 0.479 * width, y: y + 0.455 * height }, // Checkpoint 5
        { x: x + 0.696 * width, y: y + 0.848 * height }, // Checkpoint 6
        { x: x + 0.768 * width, y: y + 0.483 * height }, // Checkpoint 7
        { x: x + 0.931 * width, y: y + 0.835 * height }, // Checkpoint 8
        { x: x + 0.966 * width, y: y + -0.012 * height }, // Checkpoint 9
        { x: x + 0.921 * width, y: y + 0.011 * height }, // Checkpoint 10
        { x: x + 0.004 * width, y: y + 0.031 * height }, // Back to Start
    ];
}

function generate311Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.502 * width, y: y + 0.498 * height, angle: 180 }, // Start/Finish
        { x: x + 0.379 * width, y: y + 0.498 * height }, // Checkpoint 2
        { x: x + 0.231 * width, y: y + 0.563 * height }, // Checkpoint 3
        { x: x + 0.167 * width, y: y + 0.759 * height }, // Checkpoint 4
        { x: x + 0.224 * width, y: y + 0.865 * height }, // Checkpoint 5
        { x: x + 0.755 * width, y: y + 0.909 * height }, // Checkpoint 6
        { x: x + 0.931 * width, y: y + 0.846 * height }, // Checkpoint 7
        { x: x + 1.067 * width, y: y + 0.727 * height }, // Checkpoint 8
        { x: x + 1.181 * width, y: y + 0.523 * height }, // Checkpoint 9
        { x: x + 1.193 * width, y: y + 0.275 * height }, // Checkpoint 10
        { x: x + 1.126 * width, y: y + 0.101 * height }, // Checkpoint 11
        { x: x + 0.993 * width, y: y + -0.062 * height }, // Checkpoint 12
        { x: x + 0.848 * width, y: y + -0.147 * height }, // Checkpoint 13
        { x: x + 0.507 * width, y: y + -0.169 * height }, // Checkpoint 14
        { x: x + 0.288 * width, y: y + -0.114 * height }, // Checkpoint 15
        { x: x + 0.174 * width, y: y + -0.038 * height }, // Checkpoint 16
        { x: x + 0.067 * width, y: y + 0.112 * height }, // Checkpoint 17
        { x: x + 0.055 * width, y: y + 0.248 * height }, // Checkpoint 18
        { x: x + 0.119 * width, y: y + 0.300 * height }, // Checkpoint 19
        { x: x + 0.393 * width, y: y + 0.370 * height }, // Checkpoint 20
        { x: x + 0.507 * width, y: y + 0.476 * height }, // Back to Start
    ];
}

function generate312Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.064 * width, y: y + 0.781 * height, angle: 0 }, // Start/Finish
        { x: x + 0.921 * width, y: y + 0.773 * height }, // Checkpoint 2
        { x: x + 0.919 * width, y: y + 0.234 * height }, // Checkpoint 3
        { x: x + 1.052 * width, y: y + 0.147 * height }, // Checkpoint 4
        { x: x + 1.088 * width, y: y + 0.076 * height }, // Checkpoint 5
        { x: x + 1.031 * width, y: y + -0.095 * height }, // Checkpoint 6
        { x: x + 0.919 * width, y: y + -0.139 * height }, // Checkpoint 7
        { x: x + 0.800 * width, y: y + -0.112 * height }, // Checkpoint 8
        { x: x + 0.731 * width, y: y + -0.052 * height }, // Checkpoint 9
        { x: x + 0.614 * width, y: y + 0.253 * height }, // Checkpoint 10
        { x: x + 0.526 * width, y: y + 0.318 * height }, // Checkpoint 11
        { x: x + 0.112 * width, y: y + 0.321 * height }, // Checkpoint 12
        { x: x + 0.038 * width, y: y + 0.348 * height }, // Checkpoint 13
        { x: x + 0.093 * width, y: y + 0.503 * height }, // Checkpoint 14
        { x: x + 0.052 * width, y: y + 0.658 * height }, // Checkpoint 15
        { x: x + 0.062 * width, y: y + 0.743 * height }, // Back to Start
    ];
}

function generate313Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.003 * width, y: y + -0.112 * height, angle: 0 }, // Start/Finish
        { x: x + 0.067 * width, y: y + -0.163 * height }, // Checkpoint 2
        { x: x + 0.141 * width, y: y + -0.172 * height }, // Checkpoint 3
        { x: x + 0.284 * width, y: y + -0.098 * height }, // Checkpoint 4
        { x: x + 0.460 * width, y: y + -0.188 * height }, // Checkpoint 5
        { x: x + 0.598 * width, y: y + -0.109 * height }, // Checkpoint 6
        { x: x + 0.767 * width, y: y + -0.161 * height }, // Checkpoint 7
        { x: x + 0.887 * width, y: y + -0.063 * height }, // Checkpoint 8
        { x: x + 1.094 * width, y: y + -0.112 * height }, // Checkpoint 9
        { x: x + 1.201 * width, y: y + 0.190 * height }, // Checkpoint 10
        { x: x + 1.139 * width, y: y + 0.248 * height }, // Checkpoint 11
        { x: x + 1.132 * width, y: y + 0.288 * height }, // Checkpoint 12
        { x: x + 1.182 * width, y: y + 0.359 * height }, // Checkpoint 13
        { x: x + 1.115 * width, y: y + 0.465 * height }, // Checkpoint 14
        { x: x + 1.129 * width, y: y + 0.593 * height }, // Checkpoint 15
        { x: x + 1.072 * width, y: y + 0.648 * height }, // Checkpoint 16
        { x: x + 1.082 * width, y: y + 0.737 * height }, // Checkpoint 17
        { x: x + 1.020 * width, y: y + 0.805 * height }, // Checkpoint 18
        { x: x + 1.046 * width, y: y + 0.887 * height }, // Checkpoint 19
        { x: x + 1.022 * width, y: y + 0.922 * height }, // Checkpoint 20
        { x: x + 0.820 * width, y: y + 0.947 * height }, // Checkpoint 21
        { x: x + 0.758 * width, y: y + 0.901 * height }, // Checkpoint 22
        { x: x + 0.689 * width, y: y + 0.947 * height }, // Checkpoint 23
        { x: x + 0.603 * width, y: y + 0.914 * height }, // Checkpoint 24
        { x: x + 0.587 * width, y: y + 0.873 * height }, // Checkpoint 25
        { x: x + 0.458 * width, y: y + 0.930 * height }, // Checkpoint 26
        { x: x + 0.320 * width, y: y + 0.838 * height }, // Checkpoint 27
        { x: x + 0.220 * width, y: y + 0.882 * height }, // Checkpoint 28
        { x: x + 0.158 * width, y: y + 0.816 * height }, // Checkpoint 29
        { x: x + 0.048 * width, y: y + 0.862 * height }, // Checkpoint 30
        { x: x + -0.018 * width, y: y + 0.691 * height }, // Checkpoint 31
        { x: x + 0.044 * width, y: y + 0.639 * height }, // Checkpoint 32
        { x: x + 0.032 * width, y: y + 0.593 * height }, // Checkpoint 33
        { x: x + -0.092 * width, y: y + 0.520 * height }, // Checkpoint 34
        { x: x + -0.042 * width, y: y + 0.413 * height }, // Checkpoint 35
        { x: x + -0.104 * width, y: y + 0.356 * height }, // Checkpoint 36
        { x: x + -0.068 * width, y: y + 0.283 * height }, // Checkpoint 37
        { x: x + -0.080 * width, y: y + 0.196 * height }, // Checkpoint 38
        { x: x + -0.035 * width, y: y + 0.130 * height }, // Checkpoint 39
        { x: x + -0.035 * width, y: y + -0.079 * height }, // Checkpoint 40
        { x: x + -0.011 * width, y: y + -0.112 * height }, // Back to Start
    ];
}

function generate314Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.502 * width, y: y + 0.487 * height, angle: 4.5 }, // Start/Finish
        { x: x + 0.514 * width, y: y + 0.446 * height }, // Checkpoint 2
        { x: x + 0.409 * width, y: y + 0.166 * height }, // Checkpoint 3
        { x: x + 0.404 * width, y: y + -0.011 * height }, // Checkpoint 4
        { x: x + 0.476 * width, y: y + -0.112 * height }, // Checkpoint 5
        { x: x + 0.659 * width, y: y + -0.155 * height }, // Checkpoint 6
        { x: x + 0.728 * width, y: y + -0.117 * height }, // Checkpoint 7
        { x: x + 0.766 * width, y: y + -0.041 * height }, // Checkpoint 8
        { x: x + 0.759 * width, y: y + 0.212 * height }, // Checkpoint 9
        { x: x + 0.995 * width, y: y + 0.357 * height }, // Checkpoint 10
        { x: x + 1.076 * width, y: y + 0.452 * height }, // Checkpoint 11
        { x: x + 1.123 * width, y: y + 0.724 * height }, // Checkpoint 12
        { x: x + 1.061 * width, y: y + 0.955 * height }, // Checkpoint 13
        { x: x + 0.907 * width, y: y + 1.007 * height }, // Checkpoint 14
        { x: x + 0.630 * width, y: y + 1.034 * height }, // Checkpoint 15
        { x: x + 0.502 * width, y: y + 0.947 * height }, // Checkpoint 16
        { x: x + 0.359 * width, y: y + 0.901 * height }, // Checkpoint 17
        { x: x + 0.118 * width, y: y + 0.980 * height }, // Checkpoint 18
        { x: x + -0.039 * width, y: y + 0.982 * height }, // Checkpoint 19
        { x: x + -0.108 * width, y: y + 0.882 * height }, // Checkpoint 20
        { x: x + -0.139 * width, y: y + 0.737 * height }, // Checkpoint 21
        { x: x + -0.155 * width, y: y + -0.049 * height }, // Checkpoint 22
        { x: x + 0.099 * width, y: y + -0.065 * height }, // Checkpoint 23
        { x: x + 0.142 * width, y: y + 0.405 * height }, // Checkpoint 24
        { x: x + 0.168 * width, y: y + 0.487 * height }, // Checkpoint 25
        { x: x + 0.257 * width, y: y + 0.582 * height }, // Checkpoint 26
        { x: x + 0.404 * width, y: y + 0.618 * height }, // Checkpoint 27
        { x: x + 0.495 * width, y: y + 0.493 * height }, // Back to Start
    ];
}

function generate315Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.505 * width, y: y + 0.791 * height, angle: 5.5 }, // Start/Finish
        { x: x + 0.326 * width, y: y + 0.742 * height }, // Checkpoint 2
        { x: x + 0.190 * width, y: y + 0.538 * height }, // Checkpoint 3
        { x: x + 0.190 * width, y: y + 0.415 * height }, // Checkpoint 4
        { x: x + 0.305 * width, y: y + 0.276 * height }, // Checkpoint 5
        { x: x + 0.540 * width, y: y + 0.211 * height }, // Checkpoint 6
        { x: x + 0.748 * width, y: y + 0.301 * height }, // Checkpoint 7
        { x: x + 0.826 * width, y: y + 0.399 * height }, // Checkpoint 8
        { x: x + 0.840 * width, y: y + 0.521 * height }, // Checkpoint 9
        { x: x + 0.669 * width, y: y + 0.750 * height }, // Checkpoint 10
        { x: x + 0.476 * width, y: y + 0.799 * height }, // Back to Start
    ];
}

function generate316Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.907 * width, y: y + 1.022 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.105 * width, y: y + 0.970 * height }, // Checkpoint 2
        { x: x + 0.005 * width, y: y + 0.916 * height }, // Checkpoint 3
        { x: x + 0.045 * width, y: y + 0.361 * height }, // Checkpoint 4
        { x: x + -0.107 * width, y: y + 0.018 * height }, // Checkpoint 5
        { x: x + -0.012 * width, y: y + -0.058 * height }, // Checkpoint 6
        { x: x + 0.255 * width, y: y + -0.115 * height }, // Checkpoint 7
        { x: x + 0.879 * width, y: y + -0.099 * height }, // Checkpoint 8
        { x: x + 1.007 * width, y: y + -0.026 * height }, // Checkpoint 9
        { x: x + 1.060 * width, y: y + 0.094 * height }, // Checkpoint 10
        { x: x + 1.069 * width, y: y + 0.225 * height }, // Checkpoint 11
        { x: x + 1.033 * width, y: y + 0.317 * height }, // Checkpoint 12
        { x: x + 0.926 * width, y: y + 0.363 * height }, // Checkpoint 13
        { x: x + 0.776 * width, y: y + 0.350 * height }, // Checkpoint 14
        { x: x + 0.562 * width, y: y + 0.271 * height }, // Checkpoint 15
        { x: x + 0.440 * width, y: y + 0.290 * height }, // Checkpoint 16
        { x: x + 0.343 * width, y: y + 0.353 * height }, // Checkpoint 17
        { x: x + 0.283 * width, y: y + 0.461 * height }, // Checkpoint 18
        { x: x + 0.276 * width, y: y + 0.546 * height }, // Checkpoint 19
        { x: x + 0.314 * width, y: y + 0.630 * height }, // Checkpoint 20
        { x: x + 0.421 * width, y: y + 0.685 * height }, // Checkpoint 21
        { x: x + 0.679 * width, y: y + 0.682 * height }, // Checkpoint 22
        { x: x + 0.740 * width, y: y + 0.617 * height }, // Checkpoint 23
        { x: x + 0.855 * width, y: y + 0.584 * height }, // Checkpoint 24
        { x: x + 1.007 * width, y: y + 0.568 * height }, // Checkpoint 25
        { x: x + 1.112 * width, y: y + 0.606 * height }, // Checkpoint 26
        { x: x + 1.200 * width, y: y + 0.739 * height }, // Checkpoint 27
        { x: x + 1.190 * width, y: y + 0.864 * height }, // Checkpoint 28
        { x: x + 1.114 * width, y: y + 0.954 * height }, // Checkpoint 29
        { x: x + 0.971 * width, y: y + 1.003 * height }, // Back to Start
    ];
}

function generate317Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.071 * width, y: y + 0.074 * height, angle: 0 }, // Start/Finish
        { x: x + -0.079 * width, y: y + 0.528 * height }, // Checkpoint 2
        { x: x + -0.067 * width, y: y + 0.934 * height }, // Checkpoint 3
        { x: x + 0.702 * width, y: y + 0.972 * height }, // Checkpoint 4
        { x: x + 1.079 * width, y: y + 0.923 * height }, // Checkpoint 5
        { x: x + 1.124 * width, y: y + 0.259 * height }, // Checkpoint 6
        { x: x + 1.093 * width, y: y + 0.082 * height }, // Checkpoint 7
        { x: x + 0.598 * width, y: y + 0.065 * height }, // Checkpoint 8
        { x: x + 0.343 * width, y: y + 0.106 * height }, // Checkpoint 9
        { x: x + -0.038 * width, y: y + 0.071 * height }, // Back to Start
    ];
}

function generate318Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.251 * width, y: y + 0.296 * height, angle: 1.7 }, // Start/Finish
        { x: x + 0.452 * width, y: y + 0.305 * height }, // Checkpoint 2
        { x: x + 0.433 * width, y: y + 0.081 * height }, // Checkpoint 3
        { x: x + 0.469 * width, y: y + 0.055 * height }, // Checkpoint 4
        { x: x + 0.694 * width, y: y + 0.075 * height }, // Checkpoint 5
        { x: x + 0.650 * width, y: y + 0.341 * height }, // Checkpoint 6
        { x: x + 0.815 * width, y: y + 0.386 * height }, // Checkpoint 7
        { x: x + 0.784 * width, y: y + 0.475 * height }, // Checkpoint 8
        { x: x + 0.806 * width, y: y + 0.615 * height }, // Checkpoint 9
        { x: x + 0.585 * width, y: y + 0.563 * height }, // Checkpoint 10
        { x: x + 0.612 * width, y: y + 0.747 * height }, // Checkpoint 11
        { x: x + 0.573 * width, y: y + 0.762 * height }, // Checkpoint 12
        { x: x + 0.389 * width, y: y + 0.670 * height }, // Checkpoint 13
        { x: x + 0.365 * width, y: y + 0.626 * height }, // Checkpoint 14
        { x: x + 0.380 * width, y: y + 0.490 * height }, // Checkpoint 15
        { x: x + 0.343 * width, y: y + 0.511 * height }, // Checkpoint 16
        { x: x + 0.231 * width, y: y + 0.470 * height }, // Checkpoint 17
        { x: x + 0.240 * width, y: y + 0.325 * height }, // Back to Start
    ];
}

function generate319Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.931 * width, y: y + 0.641 * height, angle: 179 }, // Start/Finish
        { x: x + 0.205 * width, y: y + 0.649 * height }, // Checkpoint 2
        { x: x + 0.048 * width, y: y + 0.894 * height }, // Checkpoint 3
        { x: x + -0.064 * width, y: y + 0.910 * height }, // Checkpoint 4
        { x: x + -0.129 * width, y: y + 0.801 * height }, // Checkpoint 5
        { x: x + -0.162 * width, y: y + 0.537 * height }, // Checkpoint 6
        { x: x + -0.160 * width, y: y + 0.061 * height }, // Checkpoint 7
        { x: x + -0.102 * width, y: y + -0.058 * height }, // Checkpoint 8
        { x: x + 0.036 * width, y: y + -0.097 * height }, // Checkpoint 9
        { x: x + 0.488 * width, y: y + 0.015 * height }, // Checkpoint 10
        { x: x + 0.719 * width, y: y + 0.165 * height }, // Checkpoint 11
        { x: x + 0.795 * width, y: y + 0.181 * height }, // Checkpoint 12
        { x: x + 0.852 * width, y: y + 0.129 * height }, // Checkpoint 13
        { x: x + 0.931 * width, y: y + -0.072 * height }, // Checkpoint 14
        { x: x + 1.071 * width, y: y + -0.086 * height }, // Checkpoint 15
        { x: x + 1.129 * width, y: y + 0.072 * height }, // Checkpoint 16
        { x: x + 1.140 * width, y: y + 0.328 * height }, // Checkpoint 17
        { x: x + 1.174 * width, y: y + 0.782 * height }, // Checkpoint 18
        { x: x + 1.167 * width, y: y + 0.867 * height }, // Checkpoint 19
        { x: x + 1.102 * width, y: y + 0.967 * height }, // Checkpoint 20
        { x: x + 0.957 * width, y: y + 0.986 * height }, // Checkpoint 21
        { x: x + 0.926 * width, y: y + 0.962 * height }, // Checkpoint 22
        { x: x + 0.926 * width, y: y + 0.668 * height }, // Back to Start
    ];
}

function generate320Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.749 * width, y: y + 0.815 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.325 * width, y: y + 0.815 * height }, // Checkpoint 2
        { x: x + 0.253 * width, y: y + 0.786 * height }, // Checkpoint 3
        { x: x + 0.094 * width, y: y + 0.448 * height }, // Checkpoint 4
        { x: x + 0.081 * width, y: y + 0.362 * height }, // Checkpoint 5
        { x: x + 0.119 * width, y: y + 0.290 * height }, // Checkpoint 6
        { x: x + 0.266 * width, y: y + 0.170 * height }, // Checkpoint 7
        { x: x + 0.402 * width, y: y + 0.110 * height }, // Checkpoint 8
        { x: x + 0.574 * width, y: y + 0.194 * height }, // Checkpoint 9
        { x: x + 0.646 * width, y: y + 0.174 * height }, // Checkpoint 10
        { x: x + 0.700 * width, y: y + 0.119 * height }, // Checkpoint 11
        { x: x + 0.734 * width, y: y + -0.044 * height }, // Checkpoint 12
        { x: x + 0.805 * width, y: y + -0.099 * height }, // Checkpoint 13
        { x: x + 1.040 * width, y: y + -0.082 * height }, // Checkpoint 14
        { x: x + 1.076 * width, y: y + -0.006 * height }, // Checkpoint 15
        { x: x + 1.078 * width, y: y + 0.201 * height }, // Checkpoint 16
        { x: x + 0.979 * width, y: y + 0.328 * height }, // Checkpoint 17
        { x: x + 0.738 * width, y: y + 0.417 * height }, // Checkpoint 18
        { x: x + 0.723 * width, y: y + 0.587 * height }, // Checkpoint 19
        { x: x + 1.107 * width, y: y + 0.573 * height }, // Checkpoint 20
        { x: x + 1.128 * width, y: y + 0.822 * height }, // Checkpoint 21
        { x: x + 0.730 * width, y: y + 0.839 * height }, // Back to Start
    ];
}

function generate321Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.677 * width, y: y + 0.508 * height, angle: 3.15 }, // Start/Finish
        { x: x + -0.038 * width, y: y + 0.451 * height }, // Checkpoint 2
        { x: x + -0.093 * width, y: y + 0.237 * height }, // Checkpoint 3
        { x: x + -0.017 * width, y: y + 0.000 * height }, // Checkpoint 4
        { x: x + 0.178 * width, y: y + -0.029 * height }, // Checkpoint 5
        { x: x + 0.629 * width, y: y + 0.021 * height }, // Checkpoint 6
        { x: x + 1.072 * width, y: y + 0.158 * height }, // Checkpoint 7
        { x: x + 1.149 * width, y: y + 0.443 * height }, // Checkpoint 8
        { x: x + 1.139 * width, y: y + 0.719 * height }, // Checkpoint 9
        { x: x + 1.019 * width, y: y + 0.693 * height }, // Checkpoint 10
        { x: x + 0.814 * width, y: y + 0.494 * height }, // Checkpoint 11
        { x: x + 0.629 * width, y: y + 0.508 * height }, // Back to Start
    ];
}

function generate322Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.129 * width, y: y + 0.788 * height, angle: 5.5 }, // Start/Finish
        { x: x + 0.481 * width, y: y + 0.745 * height }, // Checkpoint 2
        { x: x + 0.807 * width, y: y + 0.764 * height }, // Checkpoint 3
        { x: x + 0.883 * width, y: y + 0.712 * height }, // Checkpoint 4
        { x: x + 0.938 * width, y: y + 0.541 * height }, // Checkpoint 5
        { x: x + 0.907 * width, y: y + 0.233 * height }, // Checkpoint 6
        { x: x + 1.005 * width, y: y + 0.037 * height }, // Checkpoint 7
        { x: x + 0.948 * width, y: y + -0.129 * height }, // Checkpoint 8
        { x: x + 0.874 * width, y: y + -0.159 * height }, // Checkpoint 9
        { x: x + 0.760 * width, y: y + -0.134 * height }, // Checkpoint 10
        { x: x + 0.650 * width, y: y + 0.097 * height }, // Checkpoint 11
        { x: x + 0.531 * width, y: y + 0.214 * height }, // Checkpoint 12
        { x: x + 0.186 * width, y: y + 0.258 * height }, // Checkpoint 13
        { x: x + 0.043 * width, y: y + 0.323 * height }, // Checkpoint 14
        { x: x + 0.029 * width, y: y + 0.440 * height }, // Checkpoint 15
        { x: x + 0.195 * width, y: y + 0.541 * height }, // Checkpoint 16
        { x: x + 0.205 * width, y: y + 0.584 * height }, // Checkpoint 17
        { x: x + 0.121 * width, y: y + 0.767 * height }, // Back to Start
    ];
}

function generate323Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.875 * width, y: y + 0.892 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.475 * width, y: y + 0.872 * height }, // Checkpoint 2
        { x: x + 0.006 * width, y: y + 0.892 * height }, // Checkpoint 3
        { x: x + -0.001 * width, y: y + 0.592 * height }, // Checkpoint 4
        { x: x + 0.017 * width, y: y + 0.556 * height }, // Checkpoint 5
        { x: x + 0.265 * width, y: y + 0.568 * height }, // Checkpoint 6
        { x: x + 0.286 * width, y: y + 0.300 * height }, // Checkpoint 7
        { x: x + 0.034 * width, y: y + 0.304 * height }, // Checkpoint 8
        { x: x + 0.003 * width, y: y + 0.280 * height }, // Checkpoint 9
        { x: x + 0.003 * width, y: y + 0.011 * height }, // Checkpoint 10
        { x: x + 0.104 * width, y: y + -0.061 * height }, // Checkpoint 11
        { x: x + 0.255 * width, y: y + -0.093 * height }, // Checkpoint 12
        { x: x + 0.504 * width, y: y + -0.093 * height }, // Checkpoint 13
        { x: x + 0.679 * width, y: y + -0.013 * height }, // Checkpoint 14
        { x: x + 0.735 * width, y: y + 0.108 * height }, // Checkpoint 15
        { x: x + 0.714 * width, y: y + 0.456 * height }, // Checkpoint 16
        { x: x + 0.987 * width, y: y + 0.560 * height }, // Checkpoint 17
        { x: x + 1.057 * width, y: y + 0.664 * height }, // Checkpoint 18
        { x: x + 1.060 * width, y: y + 0.808 * height }, // Checkpoint 19
        { x: x + 0.969 * width, y: y + 0.864 * height }, // Checkpoint 20
        { x: x + 0.952 * width, y: y + 0.888 * height }, // Checkpoint 21
        { x: x + 0.917 * width, y: y + 0.900 * height }, // Back to Start
    ];
}

function generate324Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.498 * width, y: y + 0.933 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.243 * width, y: y + 0.887 * height }, // Checkpoint 2
        { x: x + 0.088 * width, y: y + 0.765 * height }, // Checkpoint 3
        { x: x + -0.016 * width, y: y + 0.585 * height }, // Checkpoint 4
        { x: x + -0.031 * width, y: y + 0.381 * height }, // Checkpoint 5
        { x: x + 0.029 * width, y: y + 0.182 * height }, // Checkpoint 6
        { x: x + 0.222 * width, y: y + -0.025 * height }, // Checkpoint 7
        { x: x + 0.407 * width, y: y + -0.090 * height }, // Checkpoint 8
        { x: x + 0.596 * width, y: y + -0.104 * height }, // Checkpoint 9
        { x: x + 0.817 * width, y: y + -0.038 * height }, // Checkpoint 10
        { x: x + 0.962 * width, y: y + 0.095 * height }, // Checkpoint 11
        { x: x + 1.024 * width, y: y + 0.248 * height }, // Checkpoint 12
        { x: x + 1.031 * width, y: y + 0.413 * height }, // Checkpoint 13
        { x: x + 0.972 * width, y: y + 0.620 * height }, // Checkpoint 14
        { x: x + 0.817 * width, y: y + 0.822 * height }, // Checkpoint 15
        { x: x + 0.722 * width, y: y + 0.884 * height }, // Checkpoint 16
        { x: x + 0.510 * width, y: y + 0.947 * height }, // Back to Start
    ];
}

function generate325Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.489 * width, y: y + 0.517 * height, angle: 322 }, // Start/Finish
        { x: x + 0.586 * width, y: y + 0.440 * height }, // Checkpoint 2
        { x: x + 0.813 * width, y: y + 0.534 * height }, // Checkpoint 3
        { x: x + 0.954 * width, y: y + 0.332 * height }, // Checkpoint 4
        { x: x + 1.056 * width, y: y + 0.394 * height }, // Checkpoint 5
        { x: x + 1.050 * width, y: y + 0.110 * height }, // Checkpoint 6
        { x: x + 0.965 * width, y: y + -0.058 * height }, // Checkpoint 7
        { x: x + 0.582 * width, y: y + -0.067 * height }, // Checkpoint 8
        { x: x + 0.366 * width, y: y + 0.090 * height }, // Checkpoint 9
        { x: x + 0.046 * width, y: y + 0.094 * height }, // Checkpoint 10
        { x: x + 0.002 * width, y: y + 0.240 * height }, // Checkpoint 11
        { x: x + -0.008 * width, y: y + 0.463 * height }, // Checkpoint 12
        { x: x + -0.004 * width, y: y + 0.506 * height }, // Checkpoint 13
        { x: x + 0.123 * width, y: y + 0.632 * height }, // Checkpoint 14
        { x: x + 0.078 * width, y: y + 0.850 * height }, // Checkpoint 15
        { x: x + 0.105 * width, y: y + 0.896 * height }, // Checkpoint 16
        { x: x + 0.188 * width, y: y + 0.939 * height }, // Checkpoint 17
        { x: x + 0.287 * width, y: y + 0.935 * height }, // Checkpoint 18
        { x: x + 0.455 * width, y: y + 0.857 * height }, // Checkpoint 19
        { x: x + 0.487 * width, y: y + 0.553 * height }, // Back to Start
    ];
}

function generate326Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.403 * width, y: y + -0.138 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.059 * width, y: y + -0.174 * height }, // Checkpoint 2
        { x: x + -0.176 * width, y: y + -0.101 * height }, // Checkpoint 3
        { x: x + -0.147 * width, y: y + 0.028 * height }, // Checkpoint 4
        { x: x + 0.078 * width, y: y + 0.239 * height }, // Checkpoint 5
        { x: x + 0.132 * width, y: y + 0.421 * height }, // Checkpoint 6
        { x: x + 0.104 * width, y: y + 0.507 * height }, // Checkpoint 7
        { x: x + -0.102 * width, y: y + 0.765 * height }, // Checkpoint 8
        { x: x + -0.125 * width, y: y + 0.855 * height }, // Checkpoint 9
        { x: x + -0.105 * width, y: y + 0.948 * height }, // Checkpoint 10
        { x: x + 0.074 * width, y: y + 0.965 * height }, // Checkpoint 11
        { x: x + 0.193 * width, y: y + 0.928 * height }, // Checkpoint 12
        { x: x + 0.792 * width, y: y + 0.979 * height }, // Checkpoint 13
        { x: x + 0.910 * width, y: y + 0.951 * height }, // Checkpoint 14
        { x: x + 1.026 * width, y: y + 0.838 * height }, // Checkpoint 15
        { x: x + 1.084 * width, y: y + 0.678 * height }, // Checkpoint 16
        { x: x + 1.105 * width, y: y + 0.425 * height }, // Checkpoint 17
        { x: x + 1.064 * width, y: y + 0.159 * height }, // Checkpoint 18
        { x: x + 0.998 * width, y: y + 0.093 * height }, // Checkpoint 19
        { x: x + 0.884 * width, y: y + 0.134 * height }, // Checkpoint 20
        { x: x + 0.801 * width, y: y + 0.227 * height }, // Checkpoint 21
        { x: x + 0.644 * width, y: y + 0.570 * height }, // Checkpoint 22
        { x: x + 0.536 * width, y: y + 0.649 * height }, // Checkpoint 23
        { x: x + 0.483 * width, y: y + 0.592 * height }, // Checkpoint 24
        { x: x + 0.463 * width, y: y + 0.455 * height }, // Checkpoint 25
        { x: x + 0.571 * width, y: y + 0.167 * height }, // Checkpoint 26
        { x: x + 0.565 * width, y: y + -0.018 * height }, // Checkpoint 27
        { x: x + 0.530 * width, y: y + -0.111 * height }, // Checkpoint 28
        { x: x + 0.454 * width, y: y + -0.175 * height }, // Checkpoint 29
        { x: x + 0.337 * width, y: y + -0.135 * height }, // Back to Start
    ];
}

function generate327Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.294 * width, y: y + 0.027 * height, angle: 2 }, // Start/Finish
        { x: x + 0.254 * width, y: y + 0.147 * height }, // Checkpoint 2
        { x: x + 0.015 * width, y: y + 0.468 * height }, // Checkpoint 3
        { x: x + 0.082 * width, y: y + 0.561 * height }, // Checkpoint 4
        { x: x + 0.192 * width, y: y + 0.542 * height }, // Checkpoint 5
        { x: x + 0.251 * width, y: y + 0.724 * height }, // Checkpoint 6
        { x: x + 0.389 * width, y: y + 0.689 * height }, // Checkpoint 7
        { x: x + 0.470 * width, y: y + 0.811 * height }, // Checkpoint 8
        { x: x + 0.635 * width, y: y + 0.713 * height }, // Checkpoint 9
        { x: x + 0.746 * width, y: y + 0.602 * height }, // Checkpoint 10
        { x: x + 0.775 * width, y: y + 0.525 * height }, // Checkpoint 11
        { x: x + 0.732 * width, y: y + 0.368 * height }, // Checkpoint 12
        { x: x + 0.939 * width, y: y + 0.283 * height }, // Checkpoint 13
        { x: x + 1.032 * width, y: y + 0.055 * height }, // Checkpoint 14
        { x: x + 0.989 * width, y: y + -0.008 * height }, // Checkpoint 15
        { x: x + 0.913 * width, y: y + -0.035 * height }, // Checkpoint 16
        { x: x + 0.744 * width, y: y + 0.041 * height }, // Checkpoint 17
        { x: x + 0.551 * width, y: y + -0.081 * height }, // Checkpoint 18
        { x: x + 0.520 * width, y: y + -0.060 * height }, // Checkpoint 19
        { x: x + 0.406 * width, y: y + -0.077 * height }, // Checkpoint 20
        { x: x + 0.344 * width, y: y + -0.031 * height }, // Back to Start
    ];
}

function generate328Track(scale, centerX, centerY) {
    const width = 200 * scale;
    const height = 150 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x, y: y + height / 2, angle: 0 },                 // Start/Finish
        { x: x + width, y: y + height / 2 },                   // Right edge middle
        { x: x + width, y: y - height / 2 },                   // Top right
        { x: x, y: y - height / 2 },                           // Top left
        { x: x, y: y + height / 2, angle: 0 }                  // Back to start
    ];
}

function generate329Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.032 * width, y: y + 0.843 * height, angle: 182 }, // Start/Finish
        { x: x + -0.087 * width, y: y + 0.526 * height }, // Checkpoint 2
        { x: x + -0.019 * width, y: y + 0.003 * height }, // Checkpoint 3
        { x: x + 0.096 * width, y: y + -0.030 * height }, // Checkpoint 4
        { x: x + 0.269 * width, y: y + -0.211 * height }, // Checkpoint 5
        { x: x + 0.309 * width, y: y + -0.214 * height }, // Checkpoint 6
        { x: x + 0.513 * width, y: y + -0.036 * height }, // Checkpoint 7
        { x: x + 0.624 * width, y: y + -0.048 * height }, // Checkpoint 8
        { x: x + 0.761 * width, y: y + -0.200 * height }, // Checkpoint 9
        { x: x + 0.833 * width, y: y + -0.214 * height }, // Checkpoint 10
        { x: x + 0.910 * width, y: y + -0.134 * height }, // Checkpoint 11
        { x: x + 0.971 * width, y: y + -0.023 * height }, // Checkpoint 12
        { x: x + 1.094 * width, y: y + -0.012 * height }, // Checkpoint 13
        { x: x + 1.178 * width, y: y + -0.052 * height }, // Checkpoint 14
        { x: x + 1.206 * width, y: y + 0.174 * height }, // Checkpoint 15
        { x: x + 1.208 * width, y: y + 0.273 * height }, // Checkpoint 16
        { x: x + 1.002 * width, y: y + 0.340 * height }, // Checkpoint 17
        { x: x + 0.879 * width, y: y + 0.315 * height }, // Checkpoint 18
        { x: x + 0.628 * width, y: y + 0.238 * height }, // Checkpoint 19
        { x: x + 0.594 * width, y: y + 0.212 * height }, // Checkpoint 20
        { x: x + 0.511 * width, y: y + 0.220 * height }, // Checkpoint 21
        { x: x + 0.437 * width, y: y + 0.270 * height }, // Checkpoint 22
        { x: x + 0.339 * width, y: y + 0.283 * height }, // Checkpoint 23
        { x: x + 0.246 * width, y: y + 0.228 * height }, // Checkpoint 24
        { x: x + 0.258 * width, y: y + 0.433 * height }, // Checkpoint 25
        { x: x + 0.327 * width, y: y + 0.524 * height }, // Checkpoint 26
        { x: x + 0.574 * width, y: y + 0.519 * height }, // Checkpoint 27
        { x: x + 0.654 * width, y: y + 0.485 * height }, // Checkpoint 28
        { x: x + 0.830 * width, y: y + 0.527 * height }, // Checkpoint 29
        { x: x + 0.865 * width, y: y + 0.551 * height }, // Checkpoint 30
        { x: x + 1.038 * width, y: y + 0.651 * height }, // Checkpoint 31
        { x: x + 1.081 * width, y: y + 0.652 * height }, // Checkpoint 32
        { x: x + 1.127 * width, y: y + 0.787 * height }, // Checkpoint 33
        { x: x + 1.115 * width, y: y + 0.899 * height }, // Checkpoint 34
        { x: x + 0.999 * width, y: y + 0.955 * height }, // Checkpoint 35
        { x: x + 0.906 * width, y: y + 0.987 * height }, // Checkpoint 36
        { x: x + 0.673 * width, y: y + 0.952 * height }, // Checkpoint 37
        { x: x + 0.587 * width, y: y + 0.906 * height }, // Checkpoint 38
        { x: x + 0.517 * width, y: y + 0.994 * height }, // Checkpoint 39
        { x: x + 0.501 * width, y: y + 1.032 * height }, // Checkpoint 40
        { x: x + 0.321 * width, y: y + 0.976 * height }, // Checkpoint 41
        { x: x + 0.231 * width, y: y + 0.909 * height }, // Checkpoint 42
        { x: x + 0.090 * width, y: y + 0.878 * height }, // Checkpoint 43
        { x: x + 0.031 * width, y: y + 0.873 * height }, // Back to Start
    ];
}

function generate330Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.116 * width, y: y + -0.028 * height, angle: 328 }, // Start/Finish
        { x: x + 0.081 * width, y: y + -0.149 * height }, // Checkpoint 2
        { x: x + 0.167 * width, y: y + -0.159 * height }, // Checkpoint 3
        { x: x + 0.239 * width, y: y + -0.130 * height }, // Checkpoint 4
        { x: x + 0.249 * width, y: y + -0.009 * height }, // Checkpoint 5
        { x: x + 0.198 * width, y: y + 0.164 * height }, // Checkpoint 6
        { x: x + 0.157 * width, y: y + 0.248 * height }, // Checkpoint 7
        { x: x + 0.205 * width, y: y + 0.342 * height }, // Checkpoint 8
        { x: x + 0.238 * width, y: y + 0.365 * height }, // Checkpoint 9
        { x: x + 0.359 * width, y: y + 0.374 * height }, // Checkpoint 10
        { x: x + 0.398 * width, y: y + 0.361 * height }, // Checkpoint 11
        { x: x + 0.588 * width, y: y + 0.197 * height }, // Checkpoint 12
        { x: x + 0.660 * width, y: y + 0.122 * height }, // Checkpoint 13
        { x: x + 0.728 * width, y: y + -0.065 * height }, // Checkpoint 14
        { x: x + 0.736 * width, y: y + -0.106 * height }, // Checkpoint 15
        { x: x + 0.902 * width, y: y + -0.124 * height }, // Checkpoint 16
        { x: x + 0.993 * width, y: y + -0.118 * height }, // Checkpoint 17
        { x: x + 1.087 * width, y: y + 0.005 * height }, // Checkpoint 18
        { x: x + 1.102 * width, y: y + 0.044 * height }, // Checkpoint 19
        { x: x + 1.063 * width, y: y + 0.286 * height }, // Checkpoint 20
        { x: x + 1.048 * width, y: y + 0.373 * height }, // Checkpoint 21
        { x: x + 0.825 * width, y: y + 0.405 * height }, // Checkpoint 22
        { x: x + 0.754 * width, y: y + 0.360 * height }, // Checkpoint 23
        { x: x + 0.685 * width, y: y + 0.531 * height }, // Checkpoint 24
        { x: x + 0.654 * width, y: y + 0.627 * height }, // Checkpoint 25
        { x: x + 0.796 * width, y: y + 0.746 * height }, // Checkpoint 26
        { x: x + 0.869 * width, y: y + 0.789 * height }, // Checkpoint 27
        { x: x + 0.852 * width, y: y + 0.938 * height }, // Checkpoint 28
        { x: x + 0.882 * width, y: y + 1.019 * height }, // Checkpoint 29
        { x: x + 0.649 * width, y: y + 0.960 * height }, // Checkpoint 30
        { x: x + 0.497 * width, y: y + 0.876 * height }, // Checkpoint 31
        { x: x + 0.320 * width, y: y + 0.790 * height }, // Checkpoint 32
        { x: x + 0.257 * width, y: y + 0.728 * height }, // Checkpoint 33
        { x: x + 0.176 * width, y: y + 0.783 * height }, // Checkpoint 34
        { x: x + 0.114 * width, y: y + 0.851 * height }, // Checkpoint 35
        { x: x + 0.004 * width, y: y + 0.903 * height }, // Checkpoint 36
        { x: x + -0.027 * width, y: y + 0.929 * height }, // Checkpoint 37
        { x: x + -0.094 * width, y: y + 0.477 * height }, // Checkpoint 38
        { x: x + -0.099 * width, y: y + 0.127 * height }, // Checkpoint 39
        { x: x + -0.106 * width, y: y + 0.037 * height }, // Checkpoint 40
        { x: x + -0.105 * width, y: y + -0.009 * height }, // Back to Start
    ];
}

function generate331Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.830 * width, y: y + 0.142 * height, angle: 1.6 }, // Start/Finish
        { x: x + 0.606 * width, y: y + 0.082 * height }, // Checkpoint 2
        { x: x + 0.494 * width, y: y + 0.101 * height }, // Checkpoint 3
        { x: x + 0.428 * width, y: y + 0.191 * height }, // Checkpoint 4
        { x: x + 0.423 * width, y: y + 0.365 * height }, // Checkpoint 5
        { x: x + 0.382 * width, y: y + 0.439 * height }, // Checkpoint 6
        { x: x + 0.275 * width, y: y + 0.455 * height }, // Checkpoint 7
        { x: x + 0.132 * width, y: y + 0.411 * height }, // Checkpoint 8
        { x: x + 0.094 * width, y: y + 0.534 * height }, // Checkpoint 9
        { x: x + 0.123 * width, y: y + 0.877 * height }, // Checkpoint 10
        { x: x + 0.242 * width, y: y + 0.920 * height }, // Checkpoint 11
        { x: x + 0.561 * width, y: y + 0.909 * height }, // Checkpoint 12
        { x: x + 0.549 * width, y: y + 0.545 * height }, // Checkpoint 13
        { x: x + 0.613 * width, y: y + 0.498 * height }, // Checkpoint 14
        { x: x + 0.847 * width, y: y + 0.471 * height }, // Checkpoint 15
        { x: x + 0.847 * width, y: y + 0.164 * height }, // Back to Start
    ];
}

function generate332Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.797 * width, y: y + -0.077 * height, angle: 0 }, // Start/Finish
        { x: x + 0.801 * width, y: y + 0.407 * height }, // Checkpoint 2
        { x: x + 0.764 * width, y: y + 0.618 * height }, // Checkpoint 3
        { x: x + 0.720 * width, y: y + 0.624 * height }, // Checkpoint 4
        { x: x + 0.510 * width, y: y + 0.621 * height }, // Checkpoint 5
        { x: x + 0.483 * width, y: y + 0.593 * height }, // Checkpoint 6
        { x: x + 0.477 * width, y: y + -0.032 * height }, // Checkpoint 7
        { x: x + 0.454 * width, y: y + -0.066 * height }, // Checkpoint 8
        { x: x + 0.319 * width, y: y + -0.087 * height }, // Checkpoint 9
        { x: x + 0.120 * width, y: y + -0.066 * height }, // Checkpoint 10
        { x: x + 0.064 * width, y: y + 0.300 * height }, // Checkpoint 11
        { x: x + 0.246 * width, y: y + 0.272 * height }, // Checkpoint 12
        { x: x + 0.244 * width, y: y + 0.569 * height }, // Checkpoint 13
        { x: x + 0.098 * width, y: y + 0.578 * height }, // Checkpoint 14
        { x: x + 0.071 * width, y: y + 0.609 * height }, // Checkpoint 15
        { x: x + 0.122 * width, y: y + 0.874 * height }, // Checkpoint 16
        { x: x + 0.150 * width, y: y + 0.899 * height }, // Checkpoint 17
        { x: x + 1.017 * width, y: y + 0.877 * height }, // Checkpoint 18
        { x: x + 1.088 * width, y: y + 0.838 * height }, // Checkpoint 19
        { x: x + 1.129 * width, y: y + 0.665 * height }, // Checkpoint 20
        { x: x + 1.001 * width, y: y + 0.556 * height }, // Checkpoint 21
        { x: x + 0.985 * width, y: y + 0.509 * height }, // Checkpoint 22
        { x: x + 1.137 * width, y: y + 0.240 * height }, // Checkpoint 23
        { x: x + 1.144 * width, y: y + 0.156 * height }, // Checkpoint 24
        { x: x + 1.032 * width, y: y + 0.016 * height }, // Checkpoint 25
        { x: x + 1.109 * width, y: y + -0.096 * height }, // Checkpoint 26
        { x: x + 1.035 * width, y: y + -0.146 * height }, // Checkpoint 27
        { x: x + 0.917 * width, y: y + -0.150 * height }, // Checkpoint 28
        { x: x + 0.818 * width, y: y + -0.056 * height }, // Back to Start
    ];
}

function generate333Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.011 * width, y: y + 0.452 * height, angle: 307 }, // Start/Finish
        { x: x + 0.127 * width, y: y + 0.268 * height }, // Checkpoint 2
        { x: x + 0.617 * width, y: y + -0.122 * height }, // Checkpoint 3
        { x: x + 1.042 * width, y: y + 0.469 * height }, // Checkpoint 4
        { x: x + 0.670 * width, y: y + 0.727 * height }, // Checkpoint 5
        { x: x + 0.529 * width, y: y + 0.876 * height }, // Checkpoint 6
        { x: x + 0.494 * width, y: y + 0.806 * height }, // Checkpoint 7
        { x: x + 0.479 * width, y: y + 0.570 * height }, // Checkpoint 8
        { x: x + 0.510 * width, y: y + 0.355 * height }, // Checkpoint 9
        { x: x + 0.031 * width, y: y + 0.732 * height }, // Checkpoint 10
        { x: x + 0.004 * width, y: y + 0.447 * height }, // Back to Start
    ];
}

function generate334Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 1.013 * width, y: y + 0.147 * height, angle: 1.7 }, // Start/Finish
        { x: x + 0.304 * width, y: y + 0.145 * height }, // Checkpoint 2
        { x: x + -0.049 * width, y: y + 0.104 * height }, // Checkpoint 3
        { x: x + -0.068 * width, y: y + 0.145 * height }, // Checkpoint 4
        { x: x + 0.020 * width, y: y + 0.637 * height }, // Checkpoint 5
        { x: x + 0.409 * width, y: y + 0.561 * height }, // Checkpoint 6
        { x: x + 0.637 * width, y: y + 0.553 * height }, // Checkpoint 7
        { x: x + 0.485 * width, y: y + 0.915 * height }, // Checkpoint 8
        { x: x + 0.763 * width, y: y + 0.890 * height }, // Checkpoint 9
        { x: x + 0.923 * width, y: y + 0.591 * height }, // Checkpoint 10
        { x: x + 0.966 * width, y: y + 0.357 * height }, // Checkpoint 11
        { x: x + 1.075 * width, y: y + 0.259 * height }, // Checkpoint 12
        { x: x + 1.066 * width, y: y + 0.180 * height }, // Checkpoint 13
        { x: x + 1.004 * width, y: y + 0.156 * height }, // Back to Start
    ];
}

function generate335Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.385 * width, y: y + 0.572 * height, angle: 122.3 }, // Start/Finish
        { x: x + 0.075 * width, y: y + 0.597 * height }, // Checkpoint 2
        { x: x + 0.037 * width, y: y + 0.714 * height }, // Checkpoint 3
        { x: x + 0.199 * width, y: y + 0.774 * height }, // Checkpoint 4
        { x: x + 0.504 * width, y: y + 0.798 * height }, // Checkpoint 5
        { x: x + 0.799 * width, y: y + 0.763 * height }, // Checkpoint 6
        { x: x + 0.844 * width, y: y + 0.733 * height }, // Checkpoint 7
        { x: x + 0.897 * width, y: y + 0.605 * height }, // Checkpoint 8
        { x: x + 0.899 * width, y: y + 0.279 * height }, // Checkpoint 9
        { x: x + 0.854 * width, y: y + -0.053 * height }, // Checkpoint 10
        { x: x + 0.809 * width, y: y + -0.116 * height }, // Checkpoint 11
        { x: x + 0.563 * width, y: y + -0.130 * height }, // Checkpoint 12
        { x: x + 0.361 * width, y: y + 0.009 * height }, // Checkpoint 13
        { x: x + 0.359 * width, y: y + 0.050 * height }, // Checkpoint 14
        { x: x + 0.611 * width, y: y + 0.053 * height }, // Checkpoint 15
        { x: x + 0.651 * width, y: y + 0.170 * height }, // Checkpoint 16
        { x: x + 0.666 * width, y: y + 0.387 * height }, // Checkpoint 17
        { x: x + 0.616 * width, y: y + 0.502 * height }, // Checkpoint 18
        { x: x + 0.382 * width, y: y + 0.562 * height }, // Back to Start
    ];
}

function generate336Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.114 * width, y: y + 1.006 * height, angle: 138 }, // Start/Finish
        { x: x + -0.119 * width, y: y + 0.897 * height }, // Checkpoint 2
        { x: x + -0.111 * width, y: y + 0.798 * height }, // Checkpoint 3
        { x: x + -0.020 * width, y: y + 0.558 * height }, // Checkpoint 4
        { x: x + -0.022 * width, y: y + 0.462 * height }, // Checkpoint 5
        { x: x + -0.074 * width, y: y + 0.278 * height }, // Checkpoint 6
        { x: x + -0.050 * width, y: y + 0.246 * height }, // Checkpoint 7
        { x: x + 0.039 * width, y: y + 0.079 * height }, // Checkpoint 8
        { x: x + 0.043 * width, y: y + -0.011 * height }, // Checkpoint 9
        { x: x + 0.136 * width, y: y + -0.107 * height }, // Checkpoint 10
        { x: x + 0.178 * width, y: y + -0.128 * height }, // Checkpoint 11
        { x: x + 0.307 * width, y: y + -0.090 * height }, // Checkpoint 12
        { x: x + 0.349 * width, y: y + -0.077 * height }, // Checkpoint 13
        { x: x + 0.509 * width, y: y + -0.098 * height }, // Checkpoint 14
        { x: x + 0.619 * width, y: y + 0.032 * height }, // Checkpoint 15
        { x: x + 0.623 * width, y: y + 0.075 * height }, // Checkpoint 16
        { x: x + 0.534 * width, y: y + 0.152 * height }, // Checkpoint 17
        { x: x + 0.495 * width, y: y + 0.167 * height }, // Checkpoint 18
        { x: x + 0.394 * width, y: y + 0.194 * height }, // Checkpoint 19
        { x: x + 0.333 * width, y: y + 0.200 * height }, // Checkpoint 20
        { x: x + 0.322 * width, y: y + 0.296 * height }, // Checkpoint 21
        { x: x + 0.334 * width, y: y + 0.352 * height }, // Checkpoint 22
        { x: x + 0.359 * width, y: y + 0.421 * height }, // Checkpoint 23
        { x: x + 0.535 * width, y: y + 0.469 * height }, // Checkpoint 24
        { x: x + 0.569 * width, y: y + 0.487 * height }, // Checkpoint 25
        { x: x + 0.700 * width, y: y + 0.639 * height }, // Checkpoint 26
        { x: x + 0.818 * width, y: y + 0.702 * height }, // Checkpoint 27
        { x: x + 0.892 * width, y: y + 0.690 * height }, // Checkpoint 28
        { x: x + 0.926 * width, y: y + 0.514 * height }, // Checkpoint 29
        { x: x + 0.887 * width, y: y + 0.442 * height }, // Checkpoint 30
        { x: x + 0.845 * width, y: y + 0.343 * height }, // Checkpoint 31
        { x: x + 0.833 * width, y: y + 0.294 * height }, // Checkpoint 32
        { x: x + 0.877 * width, y: y + 0.108 * height }, // Checkpoint 33
        { x: x + 0.880 * width, y: y + 0.020 * height }, // Checkpoint 34
        { x: x + 1.037 * width, y: y + -0.060 * height }, // Checkpoint 35
        { x: x + 1.131 * width, y: y + -0.067 * height }, // Checkpoint 36
        { x: x + 1.204 * width, y: y + 0.770 * height }, // Checkpoint 37
        { x: x + 1.177 * width, y: y + 0.849 * height }, // Checkpoint 38
        { x: x + 1.040 * width, y: y + 0.923 * height }, // Checkpoint 39
        { x: x + 0.979 * width, y: y + 0.976 * height }, // Checkpoint 40
        { x: x + 0.681 * width, y: y + 1.028 * height }, // Checkpoint 41
        { x: x + 0.531 * width, y: y + 1.020 * height }, // Checkpoint 42
        { x: x + 0.389 * width, y: y + 0.870 * height }, // Checkpoint 43
        { x: x + 0.315 * width, y: y + 0.806 * height }, // Checkpoint 44
        { x: x + 0.069 * width, y: y + 0.934 * height }, // Checkpoint 45
        { x: x + -0.079 * width, y: y + 1.043 * height }, // Back to Start
    ];
}

function generate337Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.032 * width, y: y + -0.056 * height, angle: 100 }, // Start/Finish
        { x: x + -0.068 * width, y: y + 0.140 * height }, // Checkpoint 2
        { x: x + -0.042 * width, y: y + 0.217 * height }, // Checkpoint 3
        { x: x + 0.168 * width, y: y + 0.271 * height }, // Checkpoint 4
        { x: x + 0.542 * width, y: y + 0.263 * height }, // Checkpoint 5
        { x: x + 0.577 * width, y: y + 0.339 * height }, // Checkpoint 6
        { x: x + 0.477 * width, y: y + 0.467 * height }, // Checkpoint 7
        { x: x + 0.344 * width, y: y + 0.551 * height }, // Checkpoint 8
        { x: x + 0.187 * width, y: y + 0.551 * height }, // Checkpoint 9
        { x: x + 0.042 * width, y: y + 0.459 * height }, // Checkpoint 10
        { x: x + -0.032 * width, y: y + 0.461 * height }, // Checkpoint 11
        { x: x + -0.099 * width, y: y + 0.505 * height }, // Checkpoint 12
        { x: x + -0.132 * width, y: y + 0.543 * height }, // Checkpoint 13
        { x: x + -0.149 * width, y: y + 0.668 * height }, // Checkpoint 14
        { x: x + -0.120 * width, y: y + 0.810 * height }, // Checkpoint 15
        { x: x + -0.049 * width, y: y + 0.924 * height }, // Checkpoint 16
        { x: x + 0.085 * width, y: y + 0.978 * height }, // Checkpoint 17
        { x: x + 0.787 * width, y: y + 0.959 * height }, // Checkpoint 18
        { x: x + 0.870 * width, y: y + 0.987 * height }, // Checkpoint 19
        { x: x + 1.025 * width, y: y + 0.861 * height }, // Checkpoint 20
        { x: x + 1.135 * width, y: y + 0.595 * height }, // Checkpoint 21
        { x: x + 1.151 * width, y: y + 0.075 * height }, // Checkpoint 22
        { x: x + 1.118 * width, y: y + -0.129 * height }, // Checkpoint 23
        { x: x + 0.823 * width, y: y + -0.162 * height }, // Checkpoint 24
        { x: x + 0.661 * width, y: y + -0.045 * height }, // Checkpoint 25
        { x: x + 0.549 * width, y: y + -0.012 * height }, // Checkpoint 26
        { x: x + 0.296 * width, y: y + -0.140 * height }, // Checkpoint 27
        { x: x + 0.132 * width, y: y + -0.162 * height }, // Checkpoint 28
        { x: x + 0.018 * width, y: y + -0.118 * height }, // Checkpoint 29
        { x: x + -0.030 * width, y: y + -0.053 * height }, // Back to Start
    ];
}

function generate338Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.112 * width, y: y + 0.898 * height, angle: 0 }, // Start/Finish
        { x: x + -0.095 * width, y: y + 0.097 * height }, // Checkpoint 2
        { x: x + 0.128 * width, y: y + 0.260 * height }, // Checkpoint 3
        { x: x + 0.341 * width, y: y + 0.047 * height }, // Checkpoint 4
        { x: x + 0.351 * width, y: y + 0.225 * height }, // Checkpoint 5
        { x: x + 0.559 * width, y: y + 0.269 * height }, // Checkpoint 6
        { x: x + 0.602 * width, y: y + -0.055 * height }, // Checkpoint 7
        { x: x + 0.843 * width, y: y + -0.079 * height }, // Checkpoint 8
        { x: x + 1.020 * width, y: y + 0.016 * height }, // Checkpoint 9
        { x: x + 1.093 * width, y: y + 0.173 * height }, // Checkpoint 10
        { x: x + 1.039 * width, y: y + 0.225 * height }, // Checkpoint 11
        { x: x + 0.855 * width, y: y + 0.231 * height }, // Checkpoint 12
        { x: x + 0.826 * width, y: y + 0.256 * height }, // Checkpoint 13
        { x: x + 0.766 * width, y: y + 0.656 * height }, // Checkpoint 14
        { x: x + 0.713 * width, y: y + 0.868 * height }, // Checkpoint 15
        { x: x + 0.682 * width, y: y + 0.894 * height }, // Checkpoint 16
        { x: x + 0.504 * width, y: y + 0.895 * height }, // Checkpoint 17
        { x: x + 0.342 * width, y: y + 0.856 * height }, // Checkpoint 18
        { x: x + -0.089 * width, y: y + 0.890 * height }, // Back to Start
    ];
}

function generate339Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.088 * width, y: y + 0.730 * height, angle: 0 }, // Start/Finish
        { x: x + 0.233 * width, y: y + 0.733 * height }, // Checkpoint 2
        { x: x + 0.277 * width, y: y + 0.461 * height }, // Checkpoint 3
        { x: x + 0.113 * width, y: y + 0.404 * height }, // Checkpoint 4
        { x: x + 0.089 * width, y: y + 0.096 * height }, // Checkpoint 5
        { x: x + 0.511 * width, y: y + 0.084 * height }, // Checkpoint 6
        { x: x + 0.517 * width, y: y + 0.317 * height }, // Checkpoint 7
        { x: x + 0.560 * width, y: y + 0.344 * height }, // Checkpoint 8
        { x: x + 0.893 * width, y: y + 0.254 * height }, // Checkpoint 9
        { x: x + 0.917 * width, y: y + 0.319 * height }, // Checkpoint 10
        { x: x + 0.904 * width, y: y + 0.708 * height }, // Checkpoint 11
        { x: x + 0.941 * width, y: y + 0.857 * height }, // Checkpoint 12
        { x: x + 0.401 * width, y: y + 1.017 * height }, // Checkpoint 13
        { x: x + 0.247 * width, y: y + 1.013 * height }, // Checkpoint 14
        { x: x + 0.115 * width, y: y + 0.993 * height }, // Checkpoint 15
        { x: x + 0.006 * width, y: y + 0.914 * height }, // Checkpoint 16
        { x: x + -0.011 * width, y: y + 0.684 * height }, // Checkpoint 17
        { x: x + 0.071 * width, y: y + 0.727 * height }, // Back to Start
    ];
}

function generate340Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.128 * width, y: y + 0.872 * height, angle: 282 }, // Start/Finish
        { x: x + 0.194 * width, y: y + 0.724 * height }, // Checkpoint 2
        { x: x + 0.257 * width, y: y + 0.665 * height }, // Checkpoint 3
        { x: x + 0.334 * width, y: y + 0.696 * height }, // Checkpoint 4
        { x: x + 0.502 * width, y: y + 0.846 * height }, // Checkpoint 5
        { x: x + 0.589 * width, y: y + 0.865 * height }, // Checkpoint 6
        { x: x + 0.766 * width, y: y + 0.819 * height }, // Checkpoint 7
        { x: x + 0.926 * width, y: y + 0.688 * height }, // Checkpoint 8
        { x: x + 0.999 * width, y: y + 0.534 * height }, // Checkpoint 9
        { x: x + 1.018 * width, y: y + 0.287 * height }, // Checkpoint 10
        { x: x + 0.992 * width, y: y + -0.044 * height }, // Checkpoint 11
        { x: x + 0.891 * width, y: y + -0.209 * height }, // Checkpoint 12
        { x: x + 0.358 * width, y: y + -0.214 * height }, // Checkpoint 13
        { x: x + 0.330 * width, y: y + -0.186 * height }, // Checkpoint 14
        { x: x + 0.281 * width, y: y + -0.041 * height }, // Checkpoint 15
        { x: x + 0.319 * width, y: y + 0.049 * height }, // Checkpoint 16
        { x: x + 0.584 * width, y: y + 0.128 * height }, // Checkpoint 17
        { x: x + 0.644 * width, y: y + 0.175 * height }, // Checkpoint 18
        { x: x + 0.594 * width, y: y + 0.329 * height }, // Checkpoint 19
        { x: x + 0.530 * width, y: y + 0.400 * height }, // Checkpoint 20
        { x: x + 0.421 * width, y: y + 0.429 * height }, // Checkpoint 21
        { x: x + 0.181 * width, y: y + 0.289 * height }, // Checkpoint 22
        { x: x + 0.053 * width, y: y + 0.294 * height }, // Checkpoint 23
        { x: x + -0.022 * width, y: y + 0.338 * height }, // Checkpoint 24
        { x: x + -0.090 * width, y: y + 0.562 * height }, // Checkpoint 25
        { x: x + -0.071 * width, y: y + 0.991 * height }, // Checkpoint 26
        { x: x + -0.018 * width, y: y + 1.043 * height }, // Checkpoint 27
        { x: x + 0.194 * width, y: y + 1.043 * height }, // Checkpoint 28
        { x: x + 0.148 * width, y: y + 0.950 * height }, // Back to Start
    ];
}

function generate341Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.147 * width, y: y + 0.837 * height, angle: 243 }, // Start/Finish
        { x: x + 0.041 * width, y: y + 0.514 * height }, // Checkpoint 2
        { x: x + 0.019 * width, y: y + 0.243 * height }, // Checkpoint 3
        { x: x + 0.533 * width, y: y + -0.039 * height }, // Checkpoint 4
        { x: x + 0.747 * width, y: y + -0.099 * height }, // Checkpoint 5
        { x: x + 0.799 * width, y: y + 0.000 * height }, // Checkpoint 6
        { x: x + 0.708 * width, y: y + 0.341 * height }, // Checkpoint 7
        { x: x + 0.726 * width, y: y + 0.379 * height }, // Checkpoint 8
        { x: x + 0.880 * width, y: y + 0.433 * height }, // Checkpoint 9
        { x: x + 1.005 * width, y: y + 0.536 * height }, // Checkpoint 10
        { x: x + 0.942 * width, y: y + 0.861 * height }, // Checkpoint 11
        { x: x + 0.468 * width, y: y + 0.437 * height }, // Checkpoint 12
        { x: x + 0.397 * width, y: y + 0.481 * height }, // Checkpoint 13
        { x: x + 0.156 * width, y: y + 0.818 * height }, // Back to Start
    ];
}

function generate342Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.500 * width, y: y + 0.500 * height, angle: 1.4 }, // Start/Finish
        { x: x + 0.317 * width, y: y + 0.560 * height }, // Checkpoint 2
        { x: x + 0.043 * width, y: y + 0.573 * height }, // Checkpoint 3
        { x: x + 0.021 * width, y: y + 0.489 * height }, // Checkpoint 4
        { x: x + 0.133 * width, y: y + 0.380 * height }, // Checkpoint 5
        { x: x + 0.157 * width, y: y + 0.258 * height }, // Checkpoint 6
        { x: x + 0.221 * width, y: y + 0.206 * height }, // Checkpoint 7
        { x: x + 0.295 * width, y: y + 0.198 * height }, // Checkpoint 8
        { x: x + 0.386 * width, y: y + 0.279 * height }, // Checkpoint 9
        { x: x + 0.498 * width, y: y + 0.271 * height }, // Checkpoint 10
        { x: x + 0.624 * width, y: y + 0.192 * height }, // Checkpoint 11
        { x: x + 0.743 * width, y: y + 0.184 * height }, // Checkpoint 12
        { x: x + 0.812 * width, y: y + 0.217 * height }, // Checkpoint 13
        { x: x + 0.874 * width, y: y + 0.339 * height }, // Checkpoint 14
        { x: x + 0.879 * width, y: y + 0.478 * height }, // Checkpoint 15
        { x: x + 0.798 * width, y: y + 0.630 * height }, // Checkpoint 16
        { x: x + 0.695 * width, y: y + 0.690 * height }, // Checkpoint 17
        { x: x + 0.583 * width, y: y + 0.693 * height }, // Checkpoint 18
        { x: x + 0.490 * width, y: y + 0.511 * height }, // Back to Start
    ];
}

function generate343Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.343 * width, y: y + 0.157 * height, angle: 147 }, // Start/Finish
        { x: x + 0.142 * width, y: y + 0.287 * height }, // Checkpoint 2
        { x: x + 0.041 * width, y: y + 0.482 * height }, // Checkpoint 3
        { x: x + 0.028 * width, y: y + 0.654 * height }, // Checkpoint 4
        { x: x + 0.128 * width, y: y + 0.781 * height }, // Checkpoint 5
        { x: x + 0.202 * width, y: y + 0.783 * height }, // Checkpoint 6
        { x: x + 0.277 * width, y: y + 0.639 * height }, // Checkpoint 7
        { x: x + 0.377 * width, y: y + 0.581 * height }, // Checkpoint 8
        { x: x + 0.709 * width, y: y + 0.574 * height }, // Checkpoint 9
        { x: x + 0.798 * width, y: y + 0.431 * height }, // Checkpoint 10
        { x: x + 0.805 * width, y: y + 0.307 * height }, // Checkpoint 11
        { x: x + 0.726 * width, y: y + 0.222 * height }, // Checkpoint 12
        { x: x + 0.457 * width, y: y + 0.240 * height }, // Checkpoint 13
        { x: x + 0.356 * width, y: y + 0.179 * height }, // Back to Start
    ];
}

function generate344Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.363 * width, y: y + 0.211 * height, angle: 272 }, // Start/Finish
        { x: x + 0.366 * width, y: y + 0.129 * height }, // Checkpoint 2
        { x: x + 0.311 * width, y: y + 0.006 * height }, // Checkpoint 3
        { x: x + 0.244 * width, y: y + -0.034 * height }, // Checkpoint 4
        { x: x + 0.170 * width, y: y + -0.007 * height }, // Checkpoint 5
        { x: x + 0.082 * width, y: y + 0.374 * height }, // Checkpoint 6
        { x: x + 0.082 * width, y: y + 0.562 * height }, // Checkpoint 7
        { x: x + 0.118 * width, y: y + 0.643 * height }, // Checkpoint 8
        { x: x + 0.292 * width, y: y + 0.725 * height }, // Checkpoint 9
        { x: x + 0.675 * width, y: y + 0.777 * height }, // Checkpoint 10
        { x: x + 0.797 * width, y: y + 0.768 * height }, // Checkpoint 11
        { x: x + 0.937 * width, y: y + 0.684 * height }, // Checkpoint 12
        { x: x + 1.001 * width, y: y + 0.464 * height }, // Checkpoint 13
        { x: x + 0.966 * width, y: y + 0.344 * height }, // Checkpoint 14
        { x: x + 0.799 * width, y: y + 0.287 * height }, // Checkpoint 15
        { x: x + 0.561 * width, y: y + 0.532 * height }, // Checkpoint 16
        { x: x + 0.404 * width, y: y + 0.543 * height }, // Checkpoint 17
        { x: x + 0.351 * width, y: y + 0.431 * height }, // Checkpoint 18
        { x: x + 0.361 * width, y: y + 0.216 * height }, // Back to Start
    ];
}

function generate345Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.155 * width, y: y + 0.782 * height, angle: 245 }, // Start/Finish
        { x: x + 0.014 * width, y: y + 0.480 * height }, // Checkpoint 2
        { x: x + -0.019 * width, y: y + 0.274 * height }, // Checkpoint 3
        { x: x + 0.007 * width, y: y + 0.113 * height }, // Checkpoint 4
        { x: x + 0.095 * width, y: y + 0.031 * height }, // Checkpoint 5
        { x: x + 0.250 * width, y: y + -0.007 * height }, // Checkpoint 6
        { x: x + 0.600 * width, y: y + 0.010 * height }, // Checkpoint 7
        { x: x + 0.729 * width, y: y + 0.157 * height }, // Checkpoint 8
        { x: x + 0.674 * width, y: y + 0.214 * height }, // Checkpoint 9
        { x: x + 0.462 * width, y: y + 0.293 * height }, // Checkpoint 10
        { x: x + 0.402 * width, y: y + 0.399 * height }, // Checkpoint 11
        { x: x + 0.638 * width, y: y + 0.527 * height }, // Checkpoint 12
        { x: x + 0.679 * width, y: y + 0.644 * height }, // Checkpoint 13
        { x: x + 0.633 * width, y: y + 0.766 * height }, // Checkpoint 14
        { x: x + 0.545 * width, y: y + 0.840 * height }, // Checkpoint 15
        { x: x + 0.360 * width, y: y + 0.864 * height }, // Checkpoint 16
        { x: x + 0.214 * width, y: y + 0.831 * height }, // Checkpoint 17
        { x: x + 0.129 * width, y: y + 0.752 * height }, // Back to Start
    ];
}

function generate346Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.068 * width, y: y + 0.090 * height, angle: 313 }, // Start/Finish
        { x: x + 0.067 * width, y: y + -0.057 * height }, // Checkpoint 2
        { x: x + 0.356 * width, y: y + 0.201 * height }, // Checkpoint 3
        { x: x + 0.640 * width, y: y + -0.064 * height }, // Checkpoint 4
        { x: x + 0.917 * width, y: y + 0.206 * height }, // Checkpoint 5
        { x: x + 1.065 * width, y: y + 0.215 * height }, // Checkpoint 6
        { x: x + 1.083 * width, y: y + 0.694 * height }, // Checkpoint 7
        { x: x + 1.060 * width, y: y + 0.777 * height }, // Checkpoint 8
        { x: x + 0.941 * width, y: y + 0.888 * height }, // Checkpoint 9
        { x: x + 0.748 * width, y: y + 0.928 * height }, // Checkpoint 10
        { x: x + 0.484 * width, y: y + 0.935 * height }, // Checkpoint 11
        { x: x + 0.522 * width, y: y + 0.660 * height }, // Checkpoint 12
        { x: x + 0.506 * width, y: y + 0.475 * height }, // Checkpoint 13
        { x: x + 0.140 * width, y: y + 0.498 * height }, // Checkpoint 14
        { x: x + 0.105 * width, y: y + 0.895 * height }, // Checkpoint 15
        { x: x + -0.094 * width, y: y + 0.917 * height }, // Checkpoint 16
        { x: x + -0.137 * width, y: y + 0.848 * height }, // Checkpoint 17
        { x: x + -0.159 * width, y: y + 0.585 * height }, // Checkpoint 18
        { x: x + -0.073 * width, y: y + 0.426 * height }, // Checkpoint 19
        { x: x + -0.143 * width, y: y + 0.271 * height }, // Checkpoint 20
        { x: x + -0.081 * width, y: y + 0.068 * height }, // Back to Start
    ];
}

function generate347Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.157 * width, y: y + 0.571 * height, angle: 90 }, // Start/Finish
        { x: x + 0.054 * width, y: y + 0.625 * height }, // Checkpoint 2
        { x: x + 0.018 * width, y: y + 0.743 * height }, // Checkpoint 3
        { x: x + 0.037 * width, y: y + 0.823 * height }, // Checkpoint 4
        { x: x + 0.129 * width, y: y + 0.901 * height }, // Checkpoint 5
        { x: x + 0.273 * width, y: y + 0.934 * height }, // Checkpoint 6
        { x: x + 0.414 * width, y: y + 0.885 * height }, // Checkpoint 7
        { x: x + 0.485 * width, y: y + 0.734 * height }, // Checkpoint 8
        { x: x + 0.517 * width, y: y + 0.469 * height }, // Checkpoint 9
        { x: x + 0.610 * width, y: y + 0.407 * height }, // Checkpoint 10
        { x: x + 0.818 * width, y: y + 0.491 * height }, // Checkpoint 11
        { x: x + 0.868 * width, y: y + 0.560 * height }, // Checkpoint 12
        { x: x + 0.878 * width, y: y + 0.812 * height }, // Checkpoint 13
        { x: x + 0.938 * width, y: y + 0.859 * height }, // Checkpoint 14
        { x: x + 1.049 * width, y: y + 0.866 * height }, // Checkpoint 15
        { x: x + 1.143 * width, y: y + 0.780 * height }, // Checkpoint 16
        { x: x + 1.204 * width, y: y + 0.576 * height }, // Checkpoint 17
        { x: x + 1.159 * width, y: y + 0.170 * height }, // Checkpoint 18
        { x: x + 1.083 * width, y: y + -0.028 * height }, // Checkpoint 19
        { x: x + 0.970 * width, y: y + -0.141 * height }, // Checkpoint 20
        { x: x + 0.826 * width, y: y + -0.154 * height }, // Checkpoint 21
        { x: x + 0.800 * width, y: y + -0.076 * height }, // Checkpoint 22
        { x: x + 0.811 * width, y: y + 0.092 * height }, // Checkpoint 23
        { x: x + 0.776 * width, y: y + 0.165 * height }, // Checkpoint 24
        { x: x + 0.712 * width, y: y + 0.198 * height }, // Checkpoint 25
        { x: x + 0.574 * width, y: y + 0.139 * height }, // Checkpoint 26
        { x: x + 0.284 * width, y: y + -0.112 * height }, // Checkpoint 27
        { x: x + 0.210 * width, y: y + -0.123 * height }, // Checkpoint 28
        { x: x + 0.106 * width, y: y + -0.085 * height }, // Checkpoint 29
        { x: x + 0.009 * width, y: y + 0.044 * height }, // Checkpoint 30
        { x: x + 0.016 * width, y: y + 0.166 * height }, // Checkpoint 31
        { x: x + 0.106 * width, y: y + 0.256 * height }, // Checkpoint 32
        { x: x + 0.242 * width, y: y + 0.203 * height }, // Checkpoint 33
        { x: x + 0.337 * width, y: y + 0.265 * height }, // Checkpoint 34
        { x: x + 0.354 * width, y: y + 0.345 * height }, // Checkpoint 35
        { x: x + 0.329 * width, y: y + 0.423 * height }, // Checkpoint 36
        { x: x + 0.185 * width, y: y + 0.568 * height }, // Back to Start
    ];
}

function generate348Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.090 * width, y: y + 0.511 * height, angle: 96 }, // Start/Finish
        { x: x + 0.071 * width, y: y + 0.698 * height }, // Checkpoint 2
        { x: x + 0.101 * width, y: y + 0.777 * height }, // Checkpoint 3
        { x: x + 0.168 * width, y: y + 0.810 * height }, // Checkpoint 4
        { x: x + 0.276 * width, y: y + 0.789 * height }, // Checkpoint 5
        { x: x + 0.499 * width, y: y + 0.611 * height }, // Checkpoint 6
        { x: x + 0.607 * width, y: y + 0.592 * height }, // Checkpoint 7
        { x: x + 0.702 * width, y: y + 0.646 * height }, // Checkpoint 8
        { x: x + 0.768 * width, y: y + 0.882 * height }, // Checkpoint 9
        { x: x + 0.840 * width, y: y + 0.893 * height }, // Checkpoint 10
        { x: x + 0.938 * width, y: y + 0.816 * height }, // Checkpoint 11
        { x: x + 0.988 * width, y: y + 0.702 * height }, // Checkpoint 12
        { x: x + 0.950 * width, y: y + 0.356 * height }, // Checkpoint 13
        { x: x + 0.892 * width, y: y + 0.303 * height }, // Checkpoint 14
        { x: x + 0.630 * width, y: y + 0.265 * height }, // Checkpoint 15
        { x: x + 0.571 * width, y: y + 0.159 * height }, // Checkpoint 16
        { x: x + 0.547 * width, y: y + -0.047 * height }, // Checkpoint 17
        { x: x + 0.425 * width, y: y + -0.135 * height }, // Checkpoint 18
        { x: x + 0.235 * width, y: y + -0.107 * height }, // Checkpoint 19
        { x: x + 0.115 * width, y: y + 0.002 * height }, // Checkpoint 20
        { x: x + 0.106 * width, y: y + 0.122 * height }, // Checkpoint 21
        { x: x + 0.330 * width, y: y + 0.300 * height }, // Checkpoint 22
        { x: x + 0.356 * width, y: y + 0.378 * height }, // Checkpoint 23
        { x: x + 0.076 * width, y: y + 0.534 * height }, // Back to Start
    ];
}

function generate349Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.027 * width, y: y + 0.839 * height, angle: 0.7 }, // Start/Finish
        { x: x + 0.076 * width, y: y + 0.980 * height }, // Checkpoint 2
        { x: x + 0.142 * width, y: y + 1.020 * height }, // Checkpoint 3
        { x: x + 0.531 * width, y: y + 1.011 * height }, // Checkpoint 4
        { x: x + 0.795 * width, y: y + 0.953 * height }, // Checkpoint 5
        { x: x + 0.961 * width, y: y + 0.839 * height }, // Checkpoint 6
        { x: x + 1.042 * width, y: y + 0.738 * height }, // Checkpoint 7
        { x: x + 1.028 * width, y: y + 0.520 * height }, // Checkpoint 8
        { x: x + 0.917 * width, y: y + 0.324 * height }, // Checkpoint 9
        { x: x + 0.837 * width, y: y + 0.289 * height }, // Checkpoint 10
        { x: x + 0.720 * width, y: y + 0.299 * height }, // Checkpoint 11
        { x: x + 0.517 * width, y: y + 0.425 * height }, // Checkpoint 12
        { x: x + 0.455 * width, y: y + 0.380 * height }, // Checkpoint 13
        { x: x + 0.363 * width, y: y + 0.243 * height }, // Checkpoint 14
        { x: x + 0.384 * width, y: y + 0.154 * height }, // Checkpoint 15
        { x: x + 0.465 * width, y: y + 0.063 * height }, // Checkpoint 16
        { x: x + 0.492 * width, y: y + -0.018 * height }, // Checkpoint 17
        { x: x + 0.479 * width, y: y + -0.103 * height }, // Checkpoint 18
        { x: x + 0.421 * width, y: y + -0.162 * height }, // Checkpoint 19
        { x: x + 0.033 * width, y: y + -0.111 * height }, // Checkpoint 20
        { x: x + -0.034 * width, y: y + -0.066 * height }, // Checkpoint 21
        { x: x + -0.108 * width, y: y + 0.050 * height }, // Checkpoint 22
        { x: x + -0.116 * width, y: y + 0.229 * height }, // Checkpoint 23
        { x: x + -0.060 * width, y: y + 0.353 * height }, // Checkpoint 24
        { x: x + 0.043 * width, y: y + 0.480 * height }, // Checkpoint 25
        { x: x + 0.139 * width, y: y + 0.538 * height }, // Checkpoint 26
        { x: x + 0.145 * width, y: y + 0.579 * height }, // Checkpoint 27
        { x: x + -0.013 * width, y: y + 0.759 * height }, // Checkpoint 28
        { x: x + -0.064 * width, y: y + 0.882 * height }, // Back to Start
    ];
}

function generate350Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.035 * width, y: y + 0.867 * height, angle: -0.3 }, // Start/Finish
        { x: x + 0.137 * width, y: y + 0.777 * height }, // Checkpoint 2
        { x: x + 0.301 * width, y: y + 0.747 * height }, // Checkpoint 3
        { x: x + 0.432 * width, y: y + 0.758 * height }, // Checkpoint 4
        { x: x + 0.589 * width, y: y + 0.834 * height }, // Checkpoint 5
        { x: x + 0.815 * width, y: y + 0.842 * height }, // Checkpoint 6
        { x: x + 0.899 * width, y: y + 0.810 * height }, // Checkpoint 7
        { x: x + 0.958 * width, y: y + 0.734 * height }, // Checkpoint 8
        { x: x + 0.987 * width, y: y + 0.633 * height }, // Checkpoint 9
        { x: x + 0.980 * width, y: y + 0.475 * height }, // Checkpoint 10
        { x: x + 0.942 * width, y: y + 0.336 * height }, // Checkpoint 11
        { x: x + 0.908 * width, y: y + 0.309 * height }, // Checkpoint 12
        { x: x + 0.699 * width, y: y + 0.519 * height }, // Checkpoint 13
        { x: x + 0.630 * width, y: y + 0.500 * height }, // Checkpoint 14
        { x: x + 0.577 * width, y: y + 0.440 * height }, // Checkpoint 15
        { x: x + 0.577 * width, y: y + 0.287 * height }, // Checkpoint 16
        { x: x + 0.694 * width, y: y + 0.040 * height }, // Checkpoint 17
        { x: x + 0.687 * width, y: y + -0.015 * height }, // Checkpoint 18
        { x: x + 0.630 * width, y: y + -0.077 * height }, // Checkpoint 19
        { x: x + 0.537 * width, y: y + -0.107 * height }, // Checkpoint 20
        { x: x + 0.399 * width, y: y + -0.083 * height }, // Checkpoint 21
        { x: x + 0.204 * width, y: y + 0.061 * height }, // Checkpoint 22
        { x: x + 0.113 * width, y: y + 0.192 * height }, // Checkpoint 23
        { x: x + 0.018 * width, y: y + 0.459 * height }, // Checkpoint 24
        { x: x + -0.042 * width, y: y + 0.853 * height }, // Back to Start
    ];
}

function generate351Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.473 * width, y: y + 0.760 * height, angle: 121 }, // Start/Finish
        { x: x + 0.351 * width, y: y + 0.967 * height }, // Checkpoint 2
        { x: x + 0.240 * width, y: y + 0.998 * height }, // Checkpoint 3
        { x: x + 0.068 * width, y: y + 0.907 * height }, // Checkpoint 4
        { x: x + -0.040 * width, y: y + 0.690 * height }, // Checkpoint 5
        { x: x + -0.065 * width, y: y + 0.424 * height }, // Checkpoint 6
        { x: x + -0.023 * width, y: y + 0.131 * height }, // Checkpoint 7
        { x: x + 0.075 * width, y: y + -0.075 * height }, // Checkpoint 8
        { x: x + 0.212 * width, y: y + -0.207 * height }, // Checkpoint 9
        { x: x + 0.751 * width, y: y + -0.211 * height }, // Checkpoint 10
        { x: x + 0.900 * width, y: y + -0.091 * height }, // Checkpoint 11
        { x: x + 0.987 * width, y: y + 0.069 * height }, // Checkpoint 12
        { x: x + 1.022 * width, y: y + 0.533 * height }, // Checkpoint 13
        { x: x + 0.989 * width, y: y + 0.743 * height }, // Checkpoint 14
        { x: x + 0.926 * width, y: y + 0.900 * height }, // Checkpoint 15
        { x: x + 0.797 * width, y: y + 0.976 * height }, // Checkpoint 16
        { x: x + 0.685 * width, y: y + 0.967 * height }, // Checkpoint 17
        { x: x + 0.524 * width, y: y + 0.748 * height }, // Back to Start
    ];
}

function generate352Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.024 * width, y: y + 0.414 * height, angle: 0 }, // Start/Finish
        { x: x + 0.007 * width, y: y + 0.637 * height }, // Checkpoint 2
        { x: x + 0.069 * width, y: y + 0.751 * height }, // Checkpoint 3
        { x: x + 0.271 * width, y: y + 0.882 * height }, // Checkpoint 4
        { x: x + 0.461 * width, y: y + 0.899 * height }, // Checkpoint 5
        { x: x + 0.794 * width, y: y + 0.560 * height }, // Checkpoint 6
        { x: x + 0.970 * width, y: y + 0.653 * height }, // Checkpoint 7
        { x: x + 1.037 * width, y: y + 0.630 * height }, // Checkpoint 8
        { x: x + 1.029 * width, y: y + -0.027 * height }, // Checkpoint 9
        { x: x + 1.008 * width, y: y + -0.062 * height }, // Checkpoint 10
        { x: x + 0.654 * width, y: y + 0.002 * height }, // Checkpoint 11
        { x: x + 0.340 * width, y: y + 0.018 * height }, // Checkpoint 12
        { x: x + 0.305 * width, y: y + 0.041 * height }, // Checkpoint 13
        { x: x + 0.191 * width, y: y + 0.400 * height }, // Checkpoint 14
        { x: x + 0.002 * width, y: y + 0.439 * height }, // Back to Start
    ];
}
function generate353Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.127 * width, y: y + 0.819 * height, angle: 0 }, // Start/Finish
        { x: x + 0.837 * width, y: y + 0.840 * height }, // Checkpoint 2
        { x: x + 0.925 * width, y: y + 0.808 * height }, // Checkpoint 3
        { x: x + 0.932 * width, y: y + 0.713 * height }, // Checkpoint 4
        { x: x + 0.884 * width, y: y + 0.617 * height }, // Checkpoint 5
        { x: x + 0.825 * width, y: y + 0.549 * height }, // Checkpoint 6
        { x: x + 0.658 * width, y: y + 0.476 * height }, // Checkpoint 7
        { x: x + 0.603 * width, y: y + 0.220 * height }, // Checkpoint 8
        { x: x + 0.472 * width, y: y + 0.070 * height }, // Checkpoint 9
        { x: x + 0.291 * width, y: y + 0.087 * height }, // Checkpoint 10
        { x: x + 0.225 * width, y: y + 0.174 * height }, // Checkpoint 11
        { x: x + 0.113 * width, y: y + 0.805 * height }, // Back to Start
    ];
}

function generate354Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.085 * width, y: y + 0.054 * height, angle: 1.2 }, // Start/Finish
        { x: x + 0.177 * width, y: y + 0.511 * height }, // Checkpoint 2
        { x: x + 0.468 * width, y: y + 0.783 * height }, // Checkpoint 3
        { x: x + 0.765 * width, y: y + 0.960 * height }, // Checkpoint 4
        { x: x + 0.877 * width, y: y + 0.993 * height }, // Checkpoint 5
        { x: x + 0.996 * width, y: y + 0.993 * height }, // Checkpoint 6
        { x: x + 1.046 * width, y: y + 0.963 * height }, // Checkpoint 7
        { x: x + 1.122 * width, y: y + 0.718 * height }, // Checkpoint 8
        { x: x + 1.139 * width, y: y + 0.326 * height }, // Checkpoint 9
        { x: x + 1.034 * width, y: y + 0.119 * height }, // Checkpoint 10
        { x: x + 0.913 * width, y: y + -0.028 * height }, // Checkpoint 11
        { x: x + 0.670 * width, y: y + -0.123 * height }, // Checkpoint 12
        { x: x + 0.308 * width, y: y + -0.060 * height }, // Checkpoint 13
        { x: x + 0.013 * width, y: y + -0.058 * height }, // Checkpoint 14
        { x: x + -0.130 * width, y: y + -0.003 * height }, // Checkpoint 15
        { x: x + -0.123 * width, y: y + 0.081 * height }, // Checkpoint 16
        { x: x + -0.051 * width, y: y + 0.125 * height }, // Back to Start
    ];
}

function generate355Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.111 * width, y: y + 0.102 * height, angle: -0.4 }, // Start/Finish
        { x: x + 0.137 * width, y: y + -0.001 * height }, // Checkpoint 2
        { x: x + 0.435 * width, y: y + -0.056 * height }, // Checkpoint 3
        { x: x + 0.787 * width, y: y + -0.053 * height }, // Checkpoint 4
        { x: x + 1.013 * width, y: y + 0.018 * height }, // Checkpoint 5
        { x: x + 1.080 * width, y: y + 0.067 * height }, // Checkpoint 6
        { x: x + 1.154 * width, y: y + 0.301 * height }, // Checkpoint 7
        { x: x + 1.163 * width, y: y + 0.763 * height }, // Checkpoint 8
        { x: x + 1.118 * width, y: y + 0.880 * height }, // Checkpoint 9
        { x: x + 1.018 * width, y: y + 0.938 * height }, // Checkpoint 10
        { x: x + 0.751 * width, y: y + 0.984 * height }, // Checkpoint 11
        { x: x + 0.215 * width, y: y + 0.984 * height }, // Checkpoint 12
        { x: x + 0.008 * width, y: y + 0.940 * height }, // Checkpoint 13
        { x: x + -0.094 * width, y: y + 0.851 * height }, // Checkpoint 14
        { x: x + -0.144 * width, y: y + 0.622 * height }, // Checkpoint 15
        { x: x + -0.180 * width, y: y + 0.195 * height }, // Checkpoint 16
        { x: x + -0.113 * width, y: y + 0.091 * height }, // Back to Start
    ];
}

function generate356Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.050 * width, y: y + 0.066 * height, angle: -0.9 }, // Start/Finish
        { x: x + 0.027 * width, y: y + 0.745 * height }, // Checkpoint 2
        { x: x + 0.073 * width, y: y + 0.815 * height }, // Checkpoint 3
        { x: x + 0.349 * width, y: y + 0.867 * height }, // Checkpoint 4
        { x: x + 0.410 * width, y: y + 0.810 * height }, // Checkpoint 5
        { x: x + 0.444 * width, y: y + 0.535 * height }, // Checkpoint 6
        { x: x + 0.502 * width, y: y + 0.495 * height }, // Checkpoint 7
        { x: x + 0.659 * width, y: y + 0.517 * height }, // Checkpoint 8
        { x: x + 0.873 * width, y: y + 0.789 * height }, // Checkpoint 9
        { x: x + 0.946 * width, y: y + 0.815 * height }, // Checkpoint 10
        { x: x + 0.988 * width, y: y + 0.797 * height }, // Checkpoint 11
        { x: x + 1.107 * width, y: y + 0.587 * height }, // Checkpoint 12
        { x: x + 1.195 * width, y: y + 0.285 * height }, // Checkpoint 13
        { x: x + 1.176 * width, y: y + 0.250 * height }, // Checkpoint 14
        { x: x + 0.831 * width, y: y + 0.132 * height }, // Checkpoint 15
        { x: x + 0.839 * width, y: y + -0.082 * height }, // Checkpoint 16
        { x: x + 0.801 * width, y: y + -0.157 * height }, // Checkpoint 17
        { x: x + 0.647 * width, y: y + -0.209 * height }, // Checkpoint 18
        { x: x + 0.119 * width, y: y + -0.196 * height }, // Checkpoint 19
        { x: x + 0.069 * width, y: y + 0.079 * height }, // Back to Start
    ];
}

function generate357Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.245 * width, y: y + 0.425 * height, angle: 305 }, // Start/Finish
        { x: x + 0.586 * width, y: y + -0.069 * height }, // Checkpoint 2
        { x: x + 0.728 * width, y: y + -0.122 * height }, // Checkpoint 3
        { x: x + 0.789 * width, y: y + -0.021 * height }, // Checkpoint 4
        { x: x + 0.774 * width, y: y + 0.395 * height }, // Checkpoint 5
        { x: x + 1.203 * width, y: y + 0.399 * height }, // Checkpoint 6
        { x: x + 1.207 * width, y: y + 0.535 * height }, // Checkpoint 7
        { x: x + 1.096 * width, y: y + 0.723 * height }, // Checkpoint 8
        { x: x + 0.927 * width, y: y + 0.876 * height }, // Checkpoint 9
        { x: x + 0.785 * width, y: y + 0.933 * height }, // Checkpoint 10
        { x: x + 0.326 * width, y: y + 0.946 * height }, // Checkpoint 11
        { x: x + 0.161 * width, y: y + 0.894 * height }, // Checkpoint 12
        { x: x + 0.035 * width, y: y + 0.789 * height }, // Checkpoint 13
        { x: x + -0.073 * width, y: y + 0.539 * height }, // Checkpoint 14
        { x: x + -0.011 * width, y: y + 0.434 * height }, // Checkpoint 15
        { x: x + 0.261 * width, y: y + 0.408 * height }, // Back to Start
    ];
}

function generate358Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 1.037 * width, y: y + 0.770 * height, angle: -1.4 }, // Start/Finish
        { x: x + 1.044 * width, y: y + 0.066 * height }, // Checkpoint 2
        { x: x + 0.963 * width, y: y + -0.102 * height }, // Checkpoint 3
        { x: x + 0.653 * width, y: y + -0.114 * height }, // Checkpoint 4
        { x: x + 0.594 * width, y: y + 0.050 * height }, // Checkpoint 5
        { x: x + 0.437 * width, y: y + 0.094 * height }, // Checkpoint 6
        { x: x + 0.301 * width, y: y + -0.074 * height }, // Checkpoint 7
        { x: x + 0.186 * width, y: y + -0.114 * height }, // Checkpoint 8
        { x: x + -0.047 * width, y: y + 0.117 * height }, // Checkpoint 9
        { x: x + -0.092 * width, y: y + 0.659 * height }, // Checkpoint 10
        { x: x + -0.064 * width, y: y + 0.747 * height }, // Checkpoint 11
        { x: x + 0.037 * width, y: y + 0.822 * height }, // Checkpoint 12
        { x: x + 0.162 * width, y: y + 0.834 * height }, // Checkpoint 13
        { x: x + 0.179 * width, y: y + 0.798 * height }, // Checkpoint 14
        { x: x + 0.190 * width, y: y + 0.707 * height }, // Checkpoint 15
        { x: x + 0.138 * width, y: y + 0.472 * height }, // Checkpoint 16
        { x: x + 0.235 * width, y: y + 0.384 * height }, // Checkpoint 17
        { x: x + 0.409 * width, y: y + 0.317 * height }, // Checkpoint 18
        { x: x + 0.493 * width, y: y + 0.340 * height }, // Checkpoint 19
        { x: x + 0.531 * width, y: y + 0.424 * height }, // Checkpoint 20
        { x: x + 0.493 * width, y: y + 0.719 * height }, // Checkpoint 21
        { x: x + 0.528 * width, y: y + 0.794 * height }, // Checkpoint 22
        { x: x + 1.023 * width, y: y + 0.735 * height }, // Checkpoint 23
        { x: x + 1.012 * width, y: y + 0.798 * height }, // Back to Start
    ];
}

function generate359Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.541 * width, y: y + -0.046 * height, angle: 1.6 }, // Start/Finish
        { x: x + 0.582 * width, y: y + 0.147 * height }, // Checkpoint 2
        { x: x + 0.556 * width, y: y + 0.560 * height }, // Checkpoint 3
        { x: x + 0.601 * width, y: y + 0.751 * height }, // Checkpoint 4
        { x: x + 0.575 * width, y: y + 1.043 * height }, // Checkpoint 5
        { x: x + 0.448 * width, y: y + 1.012 * height }, // Checkpoint 6
        { x: x + 0.394 * width, y: y + 0.805 * height }, // Checkpoint 7
        { x: x + 0.315 * width, y: y + 0.778 * height }, // Checkpoint 8
        { x: x + 0.203 * width, y: y + 0.778 * height }, // Checkpoint 9
        { x: x + 0.056 * width, y: y + 0.920 * height }, // Checkpoint 10
        { x: x + -0.016 * width, y: y + 0.909 * height }, // Checkpoint 11
        { x: x + -0.063 * width, y: y + 0.716 * height }, // Checkpoint 12
        { x: x + -0.035 * width, y: y + 0.544 * height }, // Checkpoint 13
        { x: x + 0.022 * width, y: y + 0.473 * height }, // Checkpoint 14
        { x: x + 0.201 * width, y: y + 0.441 * height }, // Checkpoint 15
        { x: x + 0.313 * width, y: y + 0.457 * height }, // Checkpoint 16
        { x: x + 0.363 * width, y: y + 0.348 * height }, // Checkpoint 17
        { x: x + 0.332 * width, y: y + 0.261 * height }, // Checkpoint 18
        { x: x + 0.182 * width, y: y + 0.141 * height }, // Checkpoint 19
        { x: x + 0.210 * width, y: y + -0.003 * height }, // Checkpoint 20
        { x: x + 0.298 * width, y: y + -0.104 * height }, // Checkpoint 21
        { x: x + 0.410 * width, y: y + -0.166 * height }, // Checkpoint 22
        { x: x + 0.484 * width, y: y + -0.169 * height }, // Checkpoint 23
        { x: x + 0.525 * width, y: y + -0.060 * height }, // Back to Start
    ];
}

function generate360Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.486 * width, y: y + 0.918 * height, angle: 0 }, // Start/Finish
        { x: x + 0.190 * width, y: y + 0.965 * height }, // Checkpoint 2
        { x: x + 0.052 * width, y: y + 0.910 * height }, // Checkpoint 3
        { x: x + -0.014 * width, y: y + 0.780 * height }, // Checkpoint 4
        { x: x + -0.060 * width, y: y + 0.521 * height }, // Checkpoint 5
        { x: x + -0.060 * width, y: y + 0.336 * height }, // Checkpoint 6
        { x: x + 0.012 * width, y: y + 0.186 * height }, // Checkpoint 7
        { x: x + 0.138 * width, y: y + 0.279 * height }, // Checkpoint 8
        { x: x + 0.298 * width, y: y + 0.298 * height }, // Checkpoint 9
        { x: x + 0.512 * width, y: y + 0.105 * height }, // Checkpoint 10
        { x: x + 0.617 * width, y: y + 0.064 * height }, // Checkpoint 11
        { x: x + 0.717 * width, y: y + 0.082 * height }, // Checkpoint 12
        { x: x + 0.812 * width, y: y + 0.151 * height }, // Checkpoint 13
        { x: x + 0.852 * width, y: y + 0.322 * height }, // Checkpoint 14
        { x: x + 0.824 * width, y: y + 0.572 * height }, // Checkpoint 15
        { x: x + 1.026 * width, y: y + 0.695 * height }, // Checkpoint 16
        { x: x + 1.071 * width, y: y + 0.853 * height }, // Checkpoint 17
        { x: x + 0.943 * width, y: y + 0.932 * height }, // Checkpoint 18
        { x: x + 0.460 * width, y: y + 0.908 * height }, // Back to Start
    ];
}

function generate361Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.126 * width, y: y + -0.098 * height, angle: 1.5 }, // Start/Finish
        { x: x + -0.123 * width, y: y + 0.966 * height }, // Checkpoint 2
        { x: x + 0.112 * width, y: y + 0.962 * height }, // Checkpoint 3
        { x: x + 0.158 * width, y: y + 0.842 * height }, // Checkpoint 4
        { x: x + 0.165 * width, y: y + 0.573 * height }, // Checkpoint 5
        { x: x + 0.221 * width, y: y + 0.528 * height }, // Checkpoint 6
        { x: x + 0.415 * width, y: y + 0.569 * height }, // Checkpoint 7
        { x: x + 0.443 * width, y: y + 0.649 * height }, // Checkpoint 8
        { x: x + 0.446 * width, y: y + 0.914 * height }, // Checkpoint 9
        { x: x + 0.492 * width, y: y + 0.978 * height }, // Checkpoint 10
        { x: x + 0.797 * width, y: y + 0.942 * height }, // Checkpoint 11
        { x: x + 0.850 * width, y: y + 0.817 * height }, // Checkpoint 12
        { x: x + 0.847 * width, y: y + 0.544 * height }, // Checkpoint 13
        { x: x + 0.878 * width, y: y + 0.524 * height }, // Checkpoint 14
        { x: x + 1.064 * width, y: y + 0.553 * height }, // Checkpoint 15
        { x: x + 1.114 * width, y: y + 0.492 * height }, // Checkpoint 16
        { x: x + 1.107 * width, y: y + 0.011 * height }, // Checkpoint 17
        { x: x + 1.071 * width, y: y + -0.106 * height }, // Checkpoint 18
        { x: x + 0.934 * width, y: y + -0.170 * height }, // Checkpoint 19
        { x: x + 0.745 * width, y: y + -0.142 * height }, // Checkpoint 20
        { x: x + 0.713 * width, y: y + -0.070 * height }, // Checkpoint 21
        { x: x + 0.734 * width, y: y + 0.239 * height }, // Checkpoint 22
        { x: x + 0.678 * width, y: y + 0.296 * height }, // Checkpoint 23
        { x: x + 0.597 * width, y: y + 0.300 * height }, // Checkpoint 24
        { x: x + 0.481 * width, y: y + 0.284 * height }, // Checkpoint 25
        { x: x + 0.457 * width, y: y + 0.251 * height }, // Checkpoint 26
        { x: x + 0.453 * width, y: y + -0.022 * height }, // Checkpoint 27
        { x: x + 0.418 * width, y: y + -0.146 * height }, // Checkpoint 28
        { x: x + 0.302 * width, y: y + -0.166 * height }, // Checkpoint 29
        { x: x + -0.126 * width, y: y + -0.126 * height }, // Back to Start
    ];
}

function generate362Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.003 * width, y: y + -0.112 * height, angle: 1.6 }, // Start/Finish
        { x: x + 0.067 * width, y: y + -0.163 * height }, // Checkpoint 2
        { x: x + 0.141 * width, y: y + -0.172 * height }, // Checkpoint 3
        { x: x + 0.284 * width, y: y + -0.098 * height }, // Checkpoint 4
        { x: x + 0.460 * width, y: y + -0.188 * height }, // Checkpoint 5
        { x: x + 0.598 * width, y: y + -0.109 * height }, // Checkpoint 6
        { x: x + 0.767 * width, y: y + -0.161 * height }, // Checkpoint 7
        { x: x + 0.887 * width, y: y + -0.063 * height }, // Checkpoint 8
        { x: x + 1.094 * width, y: y + -0.112 * height }, // Checkpoint 9
        { x: x + 1.201 * width, y: y + 0.190 * height }, // Checkpoint 10
        { x: x + 1.139 * width, y: y + 0.248 * height }, // Checkpoint 11
        { x: x + 1.132 * width, y: y + 0.288 * height }, // Checkpoint 12
        { x: x + 1.182 * width, y: y + 0.359 * height }, // Checkpoint 13
        { x: x + 1.115 * width, y: y + 0.465 * height }, // Checkpoint 14
        { x: x + 1.129 * width, y: y + 0.593 * height }, // Checkpoint 15
        { x: x + 1.072 * width, y: y + 0.648 * height }, // Checkpoint 16
        { x: x + 1.082 * width, y: y + 0.737 * height }, // Checkpoint 17
        { x: x + 1.020 * width, y: y + 0.805 * height }, // Checkpoint 18
        { x: x + 1.046 * width, y: y + 0.887 * height }, // Checkpoint 19
        { x: x + 1.022 * width, y: y + 0.922 * height }, // Checkpoint 20
        { x: x + 0.820 * width, y: y + 0.947 * height }, // Checkpoint 21
        { x: x + 0.758 * width, y: y + 0.901 * height }, // Checkpoint 22
        { x: x + 0.689 * width, y: y + 0.947 * height }, // Checkpoint 23
        { x: x + 0.603 * width, y: y + 0.914 * height }, // Checkpoint 24
        { x: x + 0.587 * width, y: y + 0.873 * height }, // Checkpoint 25
        { x: x + 0.458 * width, y: y + 0.930 * height }, // Checkpoint 26
        { x: x + 0.320 * width, y: y + 0.838 * height }, // Checkpoint 27
        { x: x + 0.220 * width, y: y + 0.882 * height }, // Checkpoint 28
        { x: x + 0.158 * width, y: y + 0.816 * height }, // Checkpoint 29
        { x: x + 0.048 * width, y: y + 0.862 * height }, // Checkpoint 30
        { x: x + -0.018 * width, y: y + 0.691 * height }, // Checkpoint 31
        { x: x + 0.044 * width, y: y + 0.639 * height }, // Checkpoint 32
        { x: x + 0.032 * width, y: y + 0.593 * height }, // Checkpoint 33
        { x: x + -0.092 * width, y: y + 0.520 * height }, // Checkpoint 34
        { x: x + -0.042 * width, y: y + 0.413 * height }, // Checkpoint 35
        { x: x + -0.104 * width, y: y + 0.356 * height }, // Checkpoint 36
        { x: x + -0.068 * width, y: y + 0.283 * height }, // Checkpoint 37
        { x: x + -0.080 * width, y: y + 0.196 * height }, // Checkpoint 38
        { x: x + -0.035 * width, y: y + 0.130 * height }, // Checkpoint 39
        { x: x + -0.035 * width, y: y + -0.079 * height }, // Checkpoint 40
        { x: x + -0.011 * width, y: y + -0.112 * height }, // Back to Start
    ];
}

function generate363Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.170 * width, y: y + 0.219 * height, angle: 2 }, // Start/Finish
        { x: x + 0.866 * width, y: y + 0.241 * height }, // Checkpoint 2
        { x: x + 1.066 * width, y: y + 0.421 * height }, // Checkpoint 3
        { x: x + 0.923 * width, y: y + 0.766 * height }, // Checkpoint 4
        { x: x + 0.820 * width, y: y + 0.804 * height }, // Checkpoint 5
        { x: x + 0.566 * width, y: y + 0.758 * height }, // Checkpoint 6
        { x: x + 0.537 * width, y: y + 0.690 * height }, // Checkpoint 7
        { x: x + 0.597 * width, y: y + 0.489 * height }, // Checkpoint 8
        { x: x + 0.582 * width, y: y + 0.451 * height }, // Checkpoint 9
        { x: x + 0.256 * width, y: y + 0.448 * height }, // Checkpoint 10
        { x: x + 0.328 * width, y: y + 0.723 * height }, // Checkpoint 11
        { x: x + 0.154 * width, y: y + 0.878 * height }, // Checkpoint 12
        { x: x + 0.087 * width, y: y + 0.878 * height }, // Checkpoint 13
        { x: x + -0.051 * width, y: y + 0.679 * height }, // Checkpoint 14
        { x: x + -0.046 * width, y: y + 0.638 * height }, // Checkpoint 15
        { x: x + 0.085 * width, y: y + 0.238 * height }, // Checkpoint 16
        { x: x + 0.104 * width, y: y + 0.203 * height }, // Checkpoint 17
        { x: x + 0.175 * width, y: y + 0.211 * height }, // Back to Start
    ];
}

function generate364Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.067 * width, y: y + 0.015 * height, angle: 2.2 }, // Start/Finish
        { x: x + -0.092 * width, y: y + 0.290 * height }, // Checkpoint 2
        { x: x + -0.085 * width, y: y + 0.472 * height }, // Checkpoint 3
        { x: x + 0.227 * width, y: y + 0.885 * height }, // Checkpoint 4
        { x: x + 0.408 * width, y: y + 0.964 * height }, // Checkpoint 5
        { x: x + 0.470 * width, y: y + 0.913 * height }, // Checkpoint 6
        { x: x + 0.494 * width, y: y + 0.828 * height }, // Checkpoint 7
        { x: x + 0.446 * width, y: y + 0.660 * height }, // Checkpoint 8
        { x: x + 0.501 * width, y: y + 0.439 * height }, // Checkpoint 9
        { x: x + 0.577 * width, y: y + 0.270 * height }, // Checkpoint 10
        { x: x + 0.732 * width, y: y + 0.075 * height }, // Checkpoint 11
        { x: x + 0.758 * width, y: y + -0.051 * height }, // Checkpoint 12
        { x: x + 0.667 * width, y: y + -0.127 * height }, // Checkpoint 13
        { x: x + 0.596 * width, y: y + -0.132 * height }, // Checkpoint 14
        { x: x + 0.303 * width, y: y + -0.083 * height }, // Checkpoint 15
        { x: x + 0.091 * width, y: y + 0.023 * height }, // Back to Start
    ];
}

function generate365Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.131 * width, y: y + 0.691 * height, angle: 0.6 }, // Start/Finish
        { x: x + 0.672 * width, y: y + -0.100 * height }, // Checkpoint 2
        { x: x + 1.045 * width, y: y + 0.232 * height }, // Checkpoint 3
        { x: x + 1.154 * width, y: y + 0.388 * height }, // Checkpoint 4
        { x: x + 0.977 * width, y: y + 0.457 * height }, // Checkpoint 5
        { x: x + 0.409 * width, y: y + 0.912 * height }, // Checkpoint 6
        { x: x + 0.118 * width, y: y + 0.672 * height }, // Back to Start
    ];
}

function generate366Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.004 * width, y: y + -0.008 * height, angle: 160 }, // Start/Finish
        { x: x + 0.211 * width, y: y + 0.012 * height }, // Checkpoint 2
        { x: x + 0.706 * width, y: y + -0.096 * height }, // Checkpoint 3
        { x: x + 0.890 * width, y: y + -0.039 * height }, // Checkpoint 4
        { x: x + 1.036 * width, y: y + 0.230 * height }, // Checkpoint 5
        { x: x + 1.020 * width, y: y + 0.744 * height }, // Checkpoint 6
        { x: x + 0.991 * width, y: y + 0.869 * height }, // Checkpoint 7
        { x: x + 0.798 * width, y: y + 0.922 * height }, // Checkpoint 8
        { x: x + 0.647 * width, y: y + 0.892 * height }, // Checkpoint 9
        { x: x + 0.624 * width, y: y + 0.772 * height }, // Checkpoint 10
        { x: x + 0.769 * width, y: y + 0.561 * height }, // Checkpoint 11
        { x: x + 0.782 * width, y: y + 0.391 * height }, // Checkpoint 12
        { x: x + 0.673 * width, y: y + 0.265 * height }, // Checkpoint 13
        { x: x + 0.485 * width, y: y + 0.210 * height }, // Checkpoint 14
        { x: x + 0.409 * width, y: y + 0.212 * height }, // Checkpoint 15
        { x: x + 0.261 * width, y: y + 0.285 * height }, // Checkpoint 16
        { x: x + 0.081 * width, y: y + 0.586 * height }, // Checkpoint 17
        { x: x + -0.022 * width, y: y + 0.654 * height }, // Checkpoint 18
        { x: x + -0.139 * width, y: y + 0.664 * height }, // Checkpoint 19
        { x: x + -0.209 * width, y: y + 0.548 * height }, // Checkpoint 20
        { x: x + -0.198 * width, y: y + 0.319 * height }, // Checkpoint 21
        { x: x + -0.132 * width, y: y + 0.059 * height }, // Checkpoint 22
        { x: x + -0.001 * width, y: y + -0.018 * height }, // Back to Start
    ];
}

function generate367Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.544 * width, y: y + 0.823 * height, angle: 2.5 }, // Start/Finish
        { x: x + 0.415 * width, y: y + 0.932 * height }, // Checkpoint 2
        { x: x + 0.296 * width, y: y + 0.954 * height }, // Checkpoint 3
        { x: x + 0.099 * width, y: y + 0.908 * height }, // Checkpoint 4
        { x: x + -0.058 * width, y: y + 0.938 * height }, // Checkpoint 5
        { x: x + -0.111 * width, y: y + 0.864 * height }, // Checkpoint 6
        { x: x + -0.118 * width, y: y + 0.772 * height }, // Checkpoint 7
        { x: x + -0.068 * width, y: y + 0.597 * height }, // Checkpoint 8
        { x: x + -0.115 * width, y: y + 0.249 * height }, // Checkpoint 9
        { x: x + 0.108 * width, y: y + -0.056 * height }, // Checkpoint 10
        { x: x + 0.227 * width, y: y + -0.028 * height }, // Checkpoint 11
        { x: x + 0.506 * width, y: y + -0.080 * height }, // Checkpoint 12
        { x: x + 0.535 * width, y: y + -0.053 * height }, // Checkpoint 13
        { x: x + 0.544 * width, y: y + 0.121 * height }, // Checkpoint 14
        { x: x + 0.573 * width, y: y + 0.154 * height }, // Checkpoint 15
        { x: x + 0.649 * width, y: y + 0.187 * height }, // Checkpoint 16
        { x: x + 0.918 * width, y: y + 0.170 * height }, // Checkpoint 17
        { x: x + 0.999 * width, y: y + 0.252 * height }, // Checkpoint 18
        { x: x + 0.989 * width, y: y + 0.342 * height }, // Checkpoint 19
        { x: x + 0.887 * width, y: y + 0.415 * height }, // Checkpoint 20
        { x: x + 0.882 * width, y: y + 0.456 * height }, // Checkpoint 21
        { x: x + 1.075 * width, y: y + 0.744 * height }, // Checkpoint 22
        { x: x + 1.032 * width, y: y + 0.867 * height }, // Checkpoint 23
        { x: x + 0.963 * width, y: y + 0.916 * height }, // Checkpoint 24
        { x: x + 0.887 * width, y: y + 0.924 * height }, // Checkpoint 25
        { x: x + 0.620 * width, y: y + 0.812 * height }, // Checkpoint 26
        { x: x + 0.549 * width, y: y + 0.815 * height }, // Checkpoint 27
        { x: x + 0.527 * width, y: y + 0.851 * height }, // Back to Start
    ];
}

function generate368Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.523 * width, y: y + 0.913 * height, angle: 4.3 }, // Start/Finish
        { x: x + -0.095 * width, y: y + 0.423 * height }, // Checkpoint 2
        { x: x + 0.231 * width, y: y + 0.372 * height }, // Checkpoint 3
        { x: x + 0.174 * width, y: y + 0.015 * height }, // Checkpoint 4
        { x: x + 0.247 * width, y: y + 0.044 * height }, // Checkpoint 5
        { x: x + 0.438 * width, y: y + 0.226 * height }, // Checkpoint 6
        { x: x + 0.556 * width, y: y + -0.203 * height }, // Checkpoint 7
        { x: x + 0.701 * width, y: y + 0.177 * height }, // Checkpoint 8
        { x: x + 1.063 * width, y: y + -0.079 * height }, // Checkpoint 9
        { x: x + 0.923 * width, y: y + 0.361 * height }, // Checkpoint 10
        { x: x + 0.947 * width, y: y + 0.396 * height }, // Checkpoint 11
        { x: x + 1.132 * width, y: y + 0.456 * height }, // Checkpoint 12
        { x: x + 1.126 * width, y: y + 0.497 * height }, // Checkpoint 13
        { x: x + 0.538 * width, y: y + 0.921 * height }, // Back to Start
    ];
}

function generate369Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.585 * width, y: y + 0.840 * height, angle: 3.14 }, // Start/Finish
        { x: x + 0.270 * width, y: y + 0.818 * height }, // Checkpoint 2
        { x: x + 0.039 * width, y: y + 0.497 * height }, // Checkpoint 3
        { x: x + 0.020 * width, y: y + 0.080 * height }, // Checkpoint 4
        { x: x + 0.273 * width, y: y + -0.099 * height }, // Checkpoint 5
        { x: x + 0.739 * width, y: y + -0.124 * height }, // Checkpoint 6
        { x: x + 1.013 * width, y: y + 0.140 * height }, // Checkpoint 7
        { x: x + 1.049 * width, y: y + 0.478 * height }, // Checkpoint 8
        { x: x + 0.865 * width, y: y + 0.649 * height }, // Checkpoint 9
        { x: x + 0.796 * width, y: y + 0.769 * height }, // Checkpoint 10
        { x: x + 0.606 * width, y: y + 0.840 * height }, // Back to Start
    ];
}

function generate370Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.461 * width, y: y + -0.080 * height, angle: 2.5 }, // Start/Finish
        { x: x + 0.249 * width, y: y + 0.061 * height }, // Checkpoint 2
        { x: x + 0.020 * width, y: y + 0.274 * height }, // Checkpoint 3
        { x: x + -0.077 * width, y: y + 0.442 * height }, // Checkpoint 4
        { x: x + -0.094 * width, y: y + 0.554 * height }, // Checkpoint 5
        { x: x + -0.037 * width, y: y + 0.709 * height }, // Checkpoint 6
        { x: x + 0.146 * width, y: y + 0.883 * height }, // Checkpoint 7
        { x: x + 0.375 * width, y: y + 0.992 * height }, // Checkpoint 8
        { x: x + 0.585 * width, y: y + 1.008 * height }, // Checkpoint 9
        { x: x + 0.668 * width, y: y + 0.967 * height }, // Checkpoint 10
        { x: x + 0.696 * width, y: y + 0.831 * height }, // Checkpoint 11
        { x: x + 0.661 * width, y: y + 0.747 * height }, // Checkpoint 12
        { x: x + 0.485 * width, y: y + 0.565 * height }, // Checkpoint 13
        { x: x + 0.463 * width, y: y + 0.396 * height }, // Checkpoint 14
        { x: x + 0.530 * width, y: y + 0.380 * height }, // Checkpoint 15
        { x: x + 0.894 * width, y: y + 0.518 * height }, // Checkpoint 16
        { x: x + 1.094 * width, y: y + 0.529 * height }, // Checkpoint 17
        { x: x + 1.154 * width, y: y + 0.478 * height }, // Checkpoint 18
        { x: x + 1.156 * width, y: y + 0.320 * height }, // Checkpoint 19
        { x: x + 1.111 * width, y: y + 0.203 * height }, // Checkpoint 20
        { x: x + 0.956 * width, y: y + 0.053 * height }, // Checkpoint 21
        { x: x + 0.804 * width, y: y + -0.007 * height }, // Checkpoint 22
        { x: x + 0.525 * width, y: y + -0.056 * height }, // Back to Start
    ];
}

function generate371Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.008 * width, y: y + 0.592 * height, angle: 4.5 }, // Start/Finish
        { x: x + -0.019 * width, y: y + 0.452 * height }, // Checkpoint 2
        { x: x + 0.004 * width, y: y + 0.215 * height }, // Checkpoint 3
        { x: x + 0.092 * width, y: y + 0.018 * height }, // Checkpoint 4
        { x: x + 0.203 * width, y: y + -0.008 * height }, // Checkpoint 5
        { x: x + 0.349 * width, y: y + 0.128 * height }, // Checkpoint 6
        { x: x + 0.360 * width, y: y + 0.272 * height }, // Checkpoint 7
        { x: x + 0.395 * width, y: y + 0.290 * height }, // Checkpoint 8
        { x: x + 0.582 * width, y: y + 0.241 * height }, // Checkpoint 9
        { x: x + 0.793 * width, y: y + 0.246 * height }, // Checkpoint 10
        { x: x + 0.916 * width, y: y + 0.386 * height }, // Checkpoint 11
        { x: x + 0.965 * width, y: y + 0.648 * height }, // Checkpoint 12
        { x: x + 0.827 * width, y: y + 0.819 * height }, // Checkpoint 13
        { x: x + 0.456 * width, y: y + 0.889 * height }, // Checkpoint 14
        { x: x + 0.402 * width, y: y + 0.832 * height }, // Checkpoint 15
        { x: x + 0.387 * width, y: y + 0.705 * height }, // Checkpoint 16
        { x: x + 0.326 * width, y: y + 0.648 * height }, // Checkpoint 17
        { x: x + 0.261 * width, y: y + 0.605 * height }, // Checkpoint 18
        { x: x + 0.061 * width, y: y + 0.592 * height }, // Back to Start
    ];
}

function generate372Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + -0.078 * width, y: y + 0.302 * height, angle: 5.6 }, // Start/Finish
        { x: x + 0.003 * width, y: y + 0.310 * height }, // Checkpoint 2
        { x: x + 0.065 * width, y: y + 0.370 * height }, // Checkpoint 3
        { x: x + 0.320 * width, y: y + 0.873 * height }, // Checkpoint 4
        { x: x + 0.551 * width, y: y + 0.645 * height }, // Checkpoint 5
        { x: x + 0.906 * width, y: y + 0.479 * height }, // Checkpoint 6
        { x: x + 0.970 * width, y: y + 0.016 * height }, // Checkpoint 7
        { x: x + 0.698 * width, y: y + 0.065 * height }, // Checkpoint 8
        { x: x + 0.239 * width, y: y + 0.038 * height }, // Checkpoint 9
        { x: x + -0.087 * width, y: y + 0.286 * height }, // Back to Start
    ];
}

function generate373Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.502 * width, y: y + 0.502 * height, angle: 278 }, // Start/Finish
        { x: x + 0.536 * width, y: y + 0.260 * height }, // Checkpoint 2
        { x: x + 0.602 * width, y: y + 0.222 * height }, // Checkpoint 3
        { x: x + 0.707 * width, y: y + 0.249 * height }, // Checkpoint 4
        { x: x + 0.755 * width, y: y + 0.312 * height }, // Checkpoint 5
        { x: x + 0.779 * width, y: y + 0.445 * height }, // Checkpoint 6
        { x: x + 0.757 * width, y: y + 0.687 * height }, // Checkpoint 7
        { x: x + 0.505 * width, y: y + 0.902 * height }, // Checkpoint 8
        { x: x + 0.276 * width, y: y + 0.932 * height }, // Checkpoint 9
        { x: x + 0.126 * width, y: y + 0.889 * height }, // Checkpoint 10
        { x: x + -0.029 * width, y: y + 0.676 * height }, // Checkpoint 11
        { x: x + -0.083 * width, y: y + 0.510 * height }, // Checkpoint 12
        { x: x + -0.102 * width, y: y + 0.246 * height }, // Checkpoint 13
        { x: x + -0.010 * width, y: y + 0.010 * height }, // Checkpoint 14
        { x: x + 0.093 * width, y: y + -0.009 * height }, // Checkpoint 15
        { x: x + 0.164 * width, y: y + 0.080 * height }, // Checkpoint 16
        { x: x + 0.148 * width, y: y + 0.459 * height }, // Checkpoint 17
        { x: x + 0.205 * width, y: y + 0.578 * height }, // Checkpoint 18
        { x: x + 0.393 * width, y: y + 0.652 * height }, // Checkpoint 19
        { x: x + 0.460 * width, y: y + 0.617 * height }, // Checkpoint 20
        { x: x + 0.500 * width, y: y + 0.502 * height }, // Back to Start
    ];
}

function generate374Track(scale, centerX, centerY) {
    // Custom track generated with Track Generator
    // WYSIWYG: Coordinates match exactly what you see in the generator
    const width = 320 * scale;
    const height = 280 * scale;
    const x = centerX - width / 2;
    const y = centerY - height / 2;

    return [
        { x: x + 0.172 * width, y: y + 0.781 * height, angle: 356 }, // Start/Finish
        { x: x + 0.647 * width, y: y + 0.745 * height }, // Checkpoint 2
        { x: x + 0.798 * width, y: y + 0.680 * height }, // Checkpoint 3
        { x: x + 0.838 * width, y: y + 0.553 * height }, // Checkpoint 4
        { x: x + 0.739 * width, y: y + 0.471 * height }, // Checkpoint 5
        { x: x + 0.559 * width, y: y + 0.399 * height }, // Checkpoint 6
        { x: x + 0.555 * width, y: y + 0.358 * height }, // Checkpoint 7
        { x: x + 0.748 * width, y: y + 0.152 * height }, // Checkpoint 8
        { x: x + 0.773 * width, y: y + 0.070 * height }, // Checkpoint 9
        { x: x + 0.744 * width, y: y + -0.004 * height }, // Checkpoint 10
        { x: x + 0.464 * width, y: y + -0.016 * height }, // Checkpoint 11
        { x: x + 0.351 * width, y: y + 0.046 * height }, // Checkpoint 12
        { x: x + 0.261 * width, y: y + 0.145 * height }, // Checkpoint 13
        { x: x + 0.286 * width, y: y + 0.397 * height }, // Checkpoint 14
        { x: x + 0.258 * width, y: y + 0.433 * height }, // Checkpoint 15
        { x: x + 0.109 * width, y: y + 0.481 * height }, // Checkpoint 16
        { x: x + 0.067 * width, y: y + 0.555 * height }, // Checkpoint 17
        { x: x + 0.124 * width, y: y + 0.762 * height }, // Checkpoint 18
        { x: x + 0.233 * width, y: y + 0.786 * height }, // Back to Start
    ];
}

const trackFunctions = {
    '298': generate298Track,
    '299': generate299Track,
    '300': generate300Track,
    '301': generate301Track,
    '302': generate302Track,
    '303': generate303Track,
    '304': generate304Track,
    '305': generate305Track,
    '306': generate306Track,
    '307': generate307Track,
    '308': generate308Track,
    '309': generate309Track,
    '310': generate310Track,
    '311': generate311Track,
    '312': generate312Track,
    '313': generate313Track,
    '314': generate314Track,
    '315': generate315Track,
    '316': generate316Track,
    '317': generate317Track,
    '318': generate318Track,
    '319': generate319Track,
    '320': generate320Track,
    '321': generate321Track,
    '322': generate322Track,
    '323': generate323Track,
    '324': generate324Track,
    '325': generate325Track,
    '326': generate326Track,
    '327': generate327Track,
    '328': generate328Track,
    '329': generate329Track,
    '330': generate330Track,
    '331': generate331Track,
    '332': generate332Track,
    '333': generate333Track,
    '334': generate334Track,
    '335': generate335Track,
    '336': generate336Track,
    '337': generate337Track,
    '338': generate338Track,
    '339': generate339Track,
    '340': generate340Track,
    '341': generate341Track,
    '342': generate342Track,
    '343': generate343Track,
    '344': generate344Track,
    '345': generate345Track,
    '346': generate346Track,
    '347': generate347Track,
    '348': generate348Track,
    '349': generate349Track,
    '350': generate350Track,
    '351': generate351Track,
    '352': generate352Track,
    '353': generate353Track,
    '354': generate354Track,
    '355': generate355Track,
    '356': generate356Track,
    '357': generate357Track,
    '358': generate358Track,
    '359': generate359Track,
    '360': generate360Track,
    '361': generate361Track,
    '362': generate362Track,
    '363': generate363Track,
    '364': generate364Track,
    '365': generate365Track,
    '366': generate366Track,
    '367': generate367Track,
    '368': generate368Track,
    '369': generate369Track,
    '370': generate370Track,
    '371': generate371Track,
    '372': generate372Track,
    '373': generate373Track,
    '374': generate374Track
};

export function getTrackFunction(trackId, centerX, centerY, scale) {
    return trackFunctions[trackId](scale, centerX, centerY) || null;
}
